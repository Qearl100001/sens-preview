# 设计还原产线 · 更新日志 · 7月6日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## AgentEval 离线评测报告

- [决策] 在 sens-preview 内新增 AgentEval 离线报告 scene，并增加 `agent-eval.html` 独立 build 入口供其他团队使用；结论卡渐变顶色用 `component-active-click-background` → `white`。
- [实现] `src/features/agent-eval/*`；`navigation-color.ts` + `ProductShellPlaceholder` 顶导换肤 token 化；路由 `/scene/agent-eval-dashboard`。
- [token] 顶导 `getThemeTopBackground`（神策绿/冰绽蓝 colorByPath）；结论卡 `component-active-click-background`；维度列 info/success/primary 近似。
- [验收] ✅已验「`npm run build` 通过（含 agent-eval.html 多入口）」| ✅已验「Playwright `/scene/agent-eval-dashboard` + `/agent-eval.html` 渲染正常」

## AgentEval 白屏修复

- [坑] 组件引用了不存在的语义 handle `error-color`，`tokenRgba` 对 undefined 调 `.replace` 导致 React 整树崩溃白屏。
- [实现] `EvalOverviewSection` / `EvalDimensionSection` 改用 `warning-color`；`EvalBadges` 低分档改用 `warning-color-active`。
- [验收] ✅已验「Playwright 无 pageerror，`#root` 有内容，「评测报告」「用例明细」可见」

## AgentEval 生产环境 Tab + 抽屉交互

- [决策] 补生产环境 Tab；KPI / 用例详情改 `SensDrawer`；维度色改为绿/蓝/紫；卡片圆角统一 `radius/l`（6px）。
- [实现] `evalProductionReportSpec.ts`、`EvalProductionSection.tsx`、`EvalCaseDetailDrawer.tsx`；`EvalDashboardPage` 双 Tab；`evalDimensionColors.ts`。
- [token] 维度绿 `success-color`、蓝 `link-color`、紫 `getColorByPath("基础色板/兰花紫/10")`；卡片 `radius/l`；待标注卡 `info-color` / `info-light-background`。
- [验收] ✅已验「`npm run build` 通过」| ✅已验「Playwright 双 Tab、KPI 抽屉、开始标注按钮可见」

## AgentEval 交互修正（KPI 内联 + 用例中抽屉）

- [决策] 生产 KPI 详情改回卡片下方内联切换；仅离线用例详情保留 `SensDrawer`（medium）；Evaluator 表改用 `sens-table`。
- [实现] `EvalProductionSection` 去掉 KPI 抽屉；`EvalCaseDetailDrawer` size=medium + Table 样式。
- [验收] ✅已验「`npm run build` 通过」

## AgentEval KPI 卡片激活态

- [实现] 激活/悬停：`component-primary` 实色边框 + `component-active-background` 浅底 + 2px 外圈光晕；底部三角指向详情区；indicator 激活用 `component-active` 加粗；详情区 `KpiDetailShell` 顶边 2.5px 主色。
- [token] `component-primary`、`component-active-background`、`component-active`、`radius/l`
- [验收] ✅已验「`npm run build` 通过」

## AgentEval 嵌套表格去外描边

- [实现] `TableShell` 增加 `borderless`；嵌套表格去掉 TableShell 外壳描边，**EvalPanel 卡片保留** `outline` 外描边 + `radius/l`（6px）。
- [验收] ✅已验「`npm run build` 通过」

## AgentEval 结论卡标题区对齐

- [实现] `EvalConclusionCard` 复用 `EvalPanel` 标题区（`spacing/3x`/`spacing/4x`、`font-size/m` + `outline` 通栏分割线），与「综合评分」标题高度一致；内容区 `padding={20}`。
- [验收] ✅已验「`npm run build` 通过」

## AgentEval 生产 KPI 详情区简化

- [决策] 去掉 KPI 激活三角与 `KpiDetailShell` 外卡片；详情区直接内联展示，子模块仍用各自 `EvalPanel`。
- [验收] ✅已验「`npm run build` 通过」

## AgentEval 区块标题辅助文案间距

- [实现] `EvalSectionTitle` 增加 `subtitle`；标题与辅助文案间距 `spacing/1x`（4px）；移除 `PanelHeadline` 负 margin 写法。
- [token] `spacing/1x`、`font-size/s`、`line-height/s`
- [验收] ✅已验「`npm run build` 通过」

## 待办 · 未完成欠账

- ⏳ standalone 交付包 README 与 serve 验法待用户录屏前再走一遍
- ⏳ 标注工作台 / Trace 下钻目标页未建（按钮保留 noop）
