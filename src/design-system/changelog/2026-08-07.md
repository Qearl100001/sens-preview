# 设计还原产线 · 更新日志 · 8月7日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## Top Navigation · 项目切换面板高度 296 + 底遮罩渐变

- [决策] 项目切换浮层总高上限 296px；超出后列表区滚动。底遮罩改用设计标注渐变：`linear-gradient(180deg, rgba(255,255,255,0) -20.69%, #FFF 77.43%)`；首屏 `scrollTop=0` 显示，滚动后隐藏。
- [实现] `TopNavigationShowcasePage`：`PROJECT_PANEL_MAX_HEIGHT=296`；列表 `maxHeight=224`（扣 pad/gap/搜索 32）；遮罩常量 `PROJECT_LIST_MASK_GRADIENT`。
- [实现] `top-navigation.md` 同步。
- [验收] ✅已验「Playwright：面板高 296 / maxH=296；列表 maxH=224 scrollH=388；遮罩含 -20.69%→77.43% 高 36；滚动后消失 / 回顶再出现」


## Top Navigation · 右上工具图标三态 + 跳转语义（14505:36662/37060/37068）

- [决策] 工具图标为功能页入口：点击跳转，不在组件内 toggle；选中态=当前所在页。热区 28×28（icon 18 + pad 5）。
- [实现] `TopNavigationShowcasePage` / `SensTopNavigation`：`activeUtilityIcon` / `onUtilityNavigate`；三态背景 `theme-top-icon-hover/active`；展示页点击本地切选中演示跳转。
- [token] `theme-top-icon-hover` · `theme-top-icon-active` · `theme-top-text*` · `radius/m`
- [验收] ✅已验「Playwright：热区 28 / icon 18 / pad 5；默认透明；悬停 `rgba(0,0,0,0.1)`；点击选中 `rgba(0,0,0,0.2)` + `aria-current=page`；帮助中心亦可跳转演示」


## Top Navigation · 诊断工具子菜单 + 工具图标 Tips

- [决策] 诊断工具子菜单（`14523:37167`）与系统语言同交互：顶对齐触发行、水平贴齐、互斥展开；项为不可选中（无 check）。右上工具行悬停用 `SensTips` bottom（导航稿 `6:7458`）；仅平台/资源/审批可持久选中。
- [实现] `TopNavigationShowcasePage`：`ACCOUNT_DIAGNOSTIC_OPTIONS` + `data-account-diagnostic-menu`；工具按钮包 `SensTips`。
- [验收] ✅已验「Playwright：诊断子菜单 5 项 deltaTop=0/gap=0；悬停通知 Tips=消息通知；平台可 active、帮助中心点击不留选中」


## Top Navigation · 去掉右上语言图标 + 点击外部收起浮层

- [决策] 语言不在右上工具行展示，仅保留账号菜单「系统语言」。所有下拉（项目/账号/主导航）点击触发源与浮层外收起。
- [实现] 移除 `nav-language` 工具项；`pointerdown` 监听 + `data-top-nav-overlay-root`。
- [验收] ✅已验「Playwright：无 nav-language（工具仅 5 项）；项目/账号/项目设置打开后 pointerdown 画布外均关闭」


## Top Navigation · 「更多」贴右 + 选中回填文案

- [决策] 「更多」永远最右侧；浮层右对齐。下拉选中项回填替换触发文案（如「智能运营」）；点击左侧其他导航后恢复「更多」。（右距随后轮改为 `spacing/4x`，见同日「更多右距 16」条。）
- [实现] `morePinnedLabel`；`data-top-navigation-more` `marginLeft:auto`；演示项含「智能运营」。
- [验收] ✅已验「Playwright：选智能运营→display 智能运营；点首页→恢复更多」（右距口径已由后续条覆盖）


## Top Navigation · 更多三层级联 + 窄屏收纳

- [决策] 更多下拉对齐导航稿 `12:5767` / `12:6683` / `226:30662`：与账号角色同系菜单；最多 3 层；子层在左侧展开并顶对齐触发行。窄屏从右往左把主导航收入更多 L1。
- [实现] `MORE_MENU_TREE` + `renderMoreCascadePanel`；`ResizeObserver` 计算 `primaryOverflowCount`；叶子选中回填。
- [验收] ✅已验「Playwright：L1 11 项；悬停分析→L2（行为/用户/其他）deltaTop=0；悬停行为分析→L3×9；选事件分析回填；窄屏 overflow=4 且 L1 前置收纳项」


## Top Navigation · 更多右距 16 + 菜单 trailing 面性 right（14519:36910）

- [决策] 推翻「更多贴右、下栏右 padding 0」：更多距右缘 `spacing/4x`（16）。更多级联与账号角色菜单有下级项的 trailing，对齐 Figma `14519:36910` / 组件 `right`（804:76）——面性实心三角，不用线性 chevron（`/icons/right.svg`）。
- [实现] 下栏 `padding: 0 spacing/4x`；`NavigationIcon` 支持 `variant`；更多/账号 trailing → `SensIcon name="right" variant="filled"`。
- [token] `spacing/4x`；图标 `filled/right`
- [验收] ✅已验「Playwright：下栏 padRight=16、更多距右 gap=16；更多展开下级 mask=`/icons/filled/right.svg`×5；账号系统语言/诊断工具 trailing 同 filled」


## Top Navigation · 功能入口菜单三态（226:29937 / 29938 / 30041 / 29988）

- [决策] 左侧九宫格打开功能入口菜单：两层（二级标题 + 三级项）时，不同二级以竖分割线分区，三级 >6 在同区内折列；单层无标题，≤6 单列、>6 折列且无分割线。列宽 200；hover/选中底色块宽 156。
- [实现] `FunctionEntryMenuPanel` + `FUNCTION_MENU_*` 演示数据；`openPanel=function` 互斥；矩阵区展示三态样张。
- [token] `theme-top-funcMenu-text*` / `background-hover|active` · `theme-top-line-light` · `radius/m` · `spacing/1x|1․5x|2x|2․5x|3x|4x|5x` · `buildShadowD4`
- [验收] ✅已验「Playwright：live grouped 三区+2 竖线；行为分析列 6+4、colW=200、bgW=156；选漏斗分析关闭并回填；矩阵 flat≤6 单列无分割线 / flat>6 两列 6+4」


## Top Navigation · 主导航「分析 / 智能运营」下拉（3:773 / 5:2414）

- [决策] 「分析」接入真实主导航两层 mega menu；「智能运营」等单层≤6 宽度自适应（不等同两层等宽）。对齐：默认菜单左缘对齐触发文字左缘；视口右侧不够时菜单右缘对齐下拉箭头右缘。
- [实现] `NAV_DROPDOWN_CONFIG`；`FunctionEntryMenuPanel` `flat-adaptive` + `width:max-content`（避免绝对定位被窄触发器卡死）；`navDropdownPlacement` + resize；默认导航项对齐 Figma IA。
- [验收] ✅已验「Playwright：分析 grouped menuW≈816 / 列 6+4+2+3 / colW=200 bgW=156 / 与文字左对齐 Δ=0；智能运营 flat-adaptive menuW≈188；窄屏 720 分析右对齐箭头 ΔRight=0」


## Top Navigation · 单层菜单左右 padding 8（226:29621）

- [决策] 一层与一层折行：白底容器左右 padding 为 `spacing/2x`（8）；hover/选中底色不再叠加第二层 8px inset（此前面板+行内各 8 → 实际 16）。
- [实现] `FunctionEntryMenuPanel`：`isFlat` 时行内 `padding=0`、底色 `width:100%`；面板水平 pad 保持 8。
- [验收] ✅已验「Playwright：用户管理 / 矩阵 flat-short / flat-wrap 均 padL=R=8 且底色距边 gap=8；两层 grouped 仍为 gap=16」


## Top Navigation · 两层单条 193 + 列间距 7（226:30007）

- [决策] 两层菜单每条信息（hover 底）宽 193；横向两列间距 7。`7` 仅顶导结构常量，不写入 foundation spacing。
- [实现] `FUNC_MENU_GROUPED_ITEM_WIDTH=193` · `FUNC_MENU_GROUPED_COLUMN_GAP=7` · 列槽 `8+193+8=209`；两层 columns `gap:7`。
- [验收] ✅已验「Playwright：分析/矩阵 grouped bgW=193、colW=209、colGap=7、行内 inset 8」


## Top Navigation · 九宫格功能入口菜单（64:9830）

- [决策] 左侧九宫格默认 IA 对齐 Figma `64:9830`：二级「最近浏览 / 热销产品」；热销项可带彩色标签（hot→旭日红、new→冰绽蓝）。主导航「分析」仍用 `FUNCTION_MENU_TWO_LEVEL`（行为分析等），不与九宫格 IA 混用。
- [实现] `FUNCTION_MENU_NINE_GRID`；`FunctionMenuItem.badge` + `SensTag` multicolor small；九宫格 live / 矩阵 `nine-grid` 接线。
- [token] `theme-top-funcMenu-*` · `theme-top-line-light` · `SensTag` 定制色标签旭日红/冰绽蓝 · `spacing/2x`（文案↔标签 gap）
- [验收] ✅已验「Playwright：live 两区+1 竖线、colW=209/bgW=193、选中绿字；hot `#FFE0E0` / new `#D7E3FC`；选漏斗分析关闭并回填；矩阵 nine-grid 同构」


## Top Navigation · 流体宽 + 上栏 16 下限（去 1280 画布滚动）

- [决策] 推翻「顶导 1280 最小画布 + 组件内横向滚动」：顶导宽度始终跟随容器拉伸；「更多」永远贴右；上栏左簇（Logo+项目）与右簇（工具+账号）最小间距 `spacing/4x`（16），到达后不再继续变窄；组件内不提供横向滚动。
- [实现] 去掉 `minWidth:1280` / `overflowX:auto`；上栏中间弹性 gap `minWidth:16`；`shellMinWidth=左+16+右+左右 pad`；下导仍按可用宽从右往左收入「更多」。
- [token] `spacing/4x`
- [验收] ✅已验「Playwright：无 scroll 容器；more 距右 16；强制父宽 520→canvas 停在 557、clusterGap=16、overflowX≠scroll」


## Top Navigation · 项目切换列表隐形滚动条

- [决策] 项目列表超出仍可滚轮/触控板滚动，系统滚动条隐藏（不占视觉宽度）。
- [实现] `top-navigation.css`：`[data-project-switcher-list]` → `scrollbar-width:none` + `::-webkit-scrollbar` 归零。
- [验收] ✅已验「Playwright：scrollbarWidth=none；scrollHeight>clientHeight；scrollTop 可变更；底遮罩滚动后消失」


## Top Navigation · 项目名超长省略 + Tips（37:8320）

- [决策] 项目选项超长单行省略（Figma `37:8320`）；仅截断项悬停用 `SensTips` 展示全名，短名不出 tip。
- [实现] `ProjectSwitcherOption`：内层 label `text-overflow:ellipsis`；canvas 测宽判定截断；截断包 `SensTips` top/center。
- [验收] ✅已验「Playwright：长项 data-truncated=true + tip 全文；短项无 tip wrap / popup=0」


## Top Navigation · 矩阵表补全 + 结构常量不升 foundation

- [决策] 角色 pill `12`/`3`、两层列间距 `7`、单条宽 `193` **不升** Radius / Spacing foundation（单场景专用，避免为顶导开 `radius/12` 或 `spacing/7`）。
- [实现] 展示页结构 / token / 结论表补齐：`funcMenu` / `proMenu` / Typography / Spacing / SensTag / SensTips；项目面板 maxH=296；上栏簇间距与工具热区；结论新增「结构常量不升 foundation」。
- [实现] `top-navigation.md` Token 表同步；去掉「更多策略未定」「pill 待确认」表述。
- [验收] 🔶待验「浏览器打开 `/components/top-navigation` 矩阵三表目视对照」

## 待办 · 未完成欠账

- 大号入口卡片分组仍未录入本页（文档写大号+小号，实现仅小号）
- Top Navigation 整体仍 Pilot / Pending
