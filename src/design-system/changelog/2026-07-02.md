# 设计还原产线 · 更新日志 · 7月2日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## Icon Foundation · 文档草稿

- [决策] Icon registry 只记录图标资产，不绑定唯一默认尺寸和默认颜色；图标尺寸跟随文字层级或组件场景，颜色跟随语义状态。
- [实现] 新增 `docs/foundations/icon.md`，记录图标库定位、结构分层、使用原则、分类、命名规范、尺寸关系、颜色规则、可点击 / 不可点击规则、第一批自定义 SVG 入库草稿、antd 外部图标、临时字符图标、图片插画边界和自查表；`docs/foundations/README.md` 加入 `icon.md`。
- [token] 文档引用 `size/icon/s`、`size/icon/m`、`text-color-transparent`、`text-sub-color-transparent`、`text-color-transparent-disable`、`link-color`、`component-active`、`warning-color`、`white`；不手改 `tokens.resolved.json` / `theme.ts`。
- [验收] ✅已验「读回 `icon.md` / `README.md`，确认 Icon Foundation 第一阶段只做文档和入库口径，不替换组件、不迁移 antd 图标。」

## Icon Foundation · Registry 入库

- [决策] 第一阶段只建立统一 registry 与 `<SensIcon />` 渲染 API，复用 `FieldIcons.tsx` / `SearchIcon.tsx` 现有 SVG 组件，不替换业务引用、不迁移 antd 图标。
- [实现] 新增 `src/design-system/icons/types.ts`（`IconName`、资产 meta、颜色/尺寸 props）、`registry.tsx`（14 个自定义 SVG 入库表）、`Icon.tsx`（`<SensIcon />`、`resolveIconColor`、`getIconSizeToken`）、`index.ts`（统一导出）。
- [token] 尺寸走 `size/icon/mini`、`size/icon/s`、`size/icon/m`；颜色走 `text-color-transparent`、`text-sub-color-transparent`、`text-color-transparent-disable`、`link-color`、`component-active`、`warning-color`、`white`；stepper 内部 10px 记为 `STEPPER_ICON_SIZE` 常量，非通用 icon token。
- [验收] ✅已验「`npm run build` 通过；14 个图标均可在 `ICON_REGISTRY` 按 name 查到；`<SensIcon name="search" sizeToken="size/icon/m" colorRole="subtle" />` API 可编译；`FieldIcons.tsx` / 业务组件引用未改动。」

## Icon Foundation · 样张页

- [决策] `/basic-styles/icon` 只做可视化验收与边界展示，不替换业务组件引用；已入库图标用 `<SensIcon />` 渲染，antd / 临时字符 / 插画分区标注为不纳入 registry。
- [实现] 新增 `src/preview/pages/basic-styles/IconBasicStylePage.tsx`；`App.tsx` 与 `PreviewShell.tsx` 注册 `/basic-styles/icon` 路由与左侧「图标」导航。
- [token] 样张展示 `size/icon/mini`、`size/icon/s`、`size/icon/m` 与颜色语义角色（`text-color-transparent`、`text-sub-color-transparent`、`text-color-transparent-disable`、`link-color`、`component-active`、`warning-color`、`white`、`currentColor`）。
- [验收] ✅已验「`npm run build` 通过；14 个已入库图标、尺寸关系、颜色角色、antd 边界、临时字符、插画边界均可在样张页结构中看到。」

## Icon Foundation · Registry 去重

- [决策] registry 只记录唯一 SVG 资产，场景别名不单独入库；`select-clear` 与 `close-circle`、`select-arrow` 与 `chevron-down` 为同一图形，usage 合并到资产条目。
- [实现] `types.ts` / `registry.tsx` 移除 `select-clear`、`select-arrow`；`chevron-down`、`close-circle` 补全 Select 场景；`icon.md` §5 / §11 / §6 同步；样张页改为展示全部 usageScenes。
- [token] 无新增 token。
- [验收] ✅已验「`npm run build` 通过；`ICON_NAMES.length === 12`；样张页不再重复展示 close-circle / select-clear。」

## Icon Foundation · Card 图标迁移完成

- [决策] 按 Figma Card `1335:26939` 标题区替换 Card 样张全部临时字符图标；只改 `CardBasicStylePage.tsx`，不迁移 Button / Input / Select / Search / antd。
- [实现] 新增 `CheckIcon`、`RenameIcon`、`WarningFilledIcon` 并入库 `check`、`rename`、`warning-filled`；Card 样张统一改用 `<SensIcon />`（`more`、`chevron-down`、`check`、`warning-filled`、`drag-vertical`、`rename`）；移除 `CardTextIcon` / `TextIcon`；`card.md` / `icon.md` 同步。
- [token] Card 图标场景使用 `size/icon/m`、`size/icon/s`、`text-sub-color-transparent`、`link-color`、`text-color-transparent-disable`、`warning-color`、`white`。
- [验收] ✅已验「`npm run build` 通过；Card 样张无临时字符图标；registry 16 个资产。」

## TikTok 数据源 Demo · 方案 B 第一批

- [决策] 录屏主路径优先：下钻页标题栏与创建抽屉迁移到 SensD 组件；`tiktok-ads-connections/` 业务层去除 antd 直接 import；Form / Toast 不新建组件，抽屉用受控字段 + 手动校验，创建成功以关抽屉与表格出现为准。
- [实现] `SensDrawer` 增加可选 `footer`；`DataSourceConnectionPage` 改用 `SensPageTitleBar` + `SearchInput`；`CreateConnectionDrawer` 改用 `SensDrawer` / `SensTitleBar` / `SensSelectDropdown` / 受控 `SensInput`·`SensTextArea`；其余 demo 页组件去除 `Typography` / `Space` / `theme.useToken()` / `message`；`DrilldownTitleBar` 改为 `SensPageTitleBar` 薄封装；删除 `.datasource-create-drawer .ant-*` CSS。
- [token] 抽屉 `radius/xl`、`buildDrawerShadow("right")`、`divideline-color-transparent-light`；页面背景 `body-background` / `white`；Typography 走 `getTypographyToken`；卡片 hover 仍用 CSS 变量 + `component-active` / `mask-01-transparent`（`!important` 留第二批）。
- [验收] ✅已验「`npm run build` 通过；`tiktok-ads-connections/` 无 antd / `@ant-design/icons` import。」
- [待做] ⏳待做「浏览器录屏全路径验收。」

## TikTok 数据源 Demo · 方案 B 第二批

- [决策] 管理页卡片按 Card Foundation：hover 仅加 `shadow/D3`，pressed 用 `component-active` 描边 + active ring；用内部 `DataSourceEntryCard`，不新建正式 DataSourceCard 组件。
- [实现] 新增 `DataSourceEntryCard.tsx`；`DataSourceLogo` 补 TikTok / Google / Meta SVG 占位；`DataSourceManagementPage` 三列网格与标题 `font-size/xxl`；下钻页搜索右对齐、空态 flex 居中；删除 `data-source-demo.css`。
- [token] `radius/l`、`divider/color/outline|light/transparent`、`buildShadowD3`、`buildActiveRingShadow("component-active-shadow")`、`background-grey`、`success-color`；Logo 48 = `spacing/6x * 2`。
- [验收] ✅已验「`npm run build` 通过。」
- [待做] ⏳待做「真实 logo 资产替换 SVG 占位；顶导渐变仍用占位 hex。」

## TikTok 数据源 Demo · 管理页 Figma 视觉还原（第一批）

- [决策] 管理页对齐 Figma `4:5963`：Sens 组件上场、入口卡改圆点状态行、TikTok 按设计稿「未接入」；下钻页与录屏数据本批不动。
- [实现] 新增 `SensLineTabs`（业务可配置 + `SensBadge` weakCount）；`DataSourceManagementShell`（220px 侧导 + 白内容区 D2←）；`DataSourceManagementPage` 改 `SensPageTitleBar` / Tab / 外置分区标题；`DataSourceEntryCard` 重写（48 logo、beta 标签、无描述）；Figma logo 落盘 `assets/logos/`；`DATA_SOURCE_INGESTION_SECTIONS` 广告生态 6 卡。
- [token] `SensPageTitleBar` 72px（当时暂用 `theme-title-background`，**见下文「SensPageTitleBar · Figma v2.1」：管理页应对齐 landing / `white`，此条口径待修正**）；Tab 徽标 `SensBadge` weakCount；卡片 `spacing/3x` padding、`outline` 12%、hover `buildShadowD3`；壳层 `body-background`、`radius/xl`、`buildShadow("D2","left")`；beta 用 `定制色/标签/山水蓝/背景/01_默认`。
- [验收] ✅已验「`npm run build` 通过；下钻路由分支未改 `DataSourceConnectionPage` / `CreateConnectionDrawer`。」
- [待做] 🔶待验「浏览器对照 Figma 管理页截图；数据库等其余分区后续补；管理页标题栏背景改 landing 后需复验。」

## TikTok 数据源 Demo · 下钻页 Figma 视觉还原（第二批）

- [决策] 下钻页与管理页共用 `DataSourceManagementShell`（顶导 + 220px 侧导 + 白内容区 D2←）；录屏仍走 `initialRecords={[]}` 空态，不改创建/表格逻辑。
- [实现] `DataSourceConnectionDemoPage` 统一壳层；`DataSourceConnectionPage` 去掉双层 body/白卡包裹，双栏 padding 对齐 scene；搜索左对齐；`DataSourceInfoPanel` 48px Figma logo + 标题行居中；`ConnectionStatusBadge` 6px 圆点 + 辅助色文案；`DataSourceSpec.logoAssetId`。
- [token] 内容区 `spacing/6x` padding；左右栏分割 `divideline-color-transparent-light`；信息区 logo `spacing/6x * 2`；状态点 `size/mini - 2`。
- [验收] ✅已验「`npm run build` 通过。」
- [待做] 🔶待验「浏览器对照 scene 截图空态/有数据两态；创建抽屉逐字段对齐。」

## TikTok 数据源 Demo · 录屏数据与创建抽屉（第三批）

- [决策] 录屏默认有数据态：下钻预置 `mockData` 2 条；管理页 TikTok 卡「已接入 · 1 个连接」；开关集中在 `dataSourceDemoConfig.ts`（`false` 可切回空态创建流程）。
- [实现] `getDemoInitialConnectionRecords()`；TikTok 卡 `connected` / `connectionCount` 随开关；抽屉 footer 主按钮改「创建」；表单区 `gap/spacing/4x`、校验错误仅走 Sens 控件 `help`（去重复报错）；账号映射区描边改 `divider/outline`；新建连接 ID 取 `max(connectionId)+1`。
- [token] 映射区 `outline` 12%、`background-transparent-grey` 4%；表单间距 `spacing/4x` / `spacing/1x`。
- [验收] ✅已验「`npm run build` 通过。」
- [待做] 🔶待验「浏览器全路径录屏：管理页 → 下钻表格 → 创建 → 返回。」

## TikTok 数据源 Demo · 入口卡 hover / pressed

- [决策] 入口卡交互与是否可下钻解耦：全部卡片走 Card Foundation default / hover(D3) / pressed(active 描边 + ring)；仅 `navigable` 卡触发下钻。
- [实现] `DataSourceEntryCard` 统一 `<button>`；hover 标题 `link-color`；补 `disabled` 态（灰底 + disabled hover D3）；Meta / Google 等无 `onClick` 仍有悬停与点击视觉。
- [token] hover `buildShadowD3`；pressed `component-active` + `buildActiveRingShadow("component-active-shadow")`；disabled `background-grey` / `divider/light`。
- [验收] ✅已验「`npm run build` 通过。」
- [待做] 🔶待验「浏览器悬停 Meta Ads / TikTok 对比 shadow 与 pressed。」

## TikTok 数据源 Demo · 下钻页无侧导

- [决策] 下钻页对齐 scene：仅保留顶导 + 全宽白内容区；管理页仍保留 220px 侧导。
- [实现] `DataSourceManagementShell` 增加 `showSideNav`；`DataSourceConnectionDemoPage` 下钻时 `showSideNav={false}`。
- [token] 无侧导时去掉 D2←，内容区补 `radius/xl` 左上圆角。
- [验收] ✅已验「`npm run build` 通过。」

## SensDrawer · 去掉 bottom footer

- [决策] 抽屉操作统一放 `SensTitleBar.actions`（标题栏右上），不提供底部 footer 槽位；对齐 `DrawerShowcasePage` 与 title-bar 设计规则。
- [实现] 移除 `SensDrawer` 的 `footer` / `footerStyle`；`CreateConnectionDrawer`「取消 / 创建」迁入 `SensTitleBar.actions`。
- [token] 内容区 padding 恢复固定 `spacing/4x` + `spacing/6x` + `spacing/6x`（上/左右/下）。
- [验收] ✅已验「`npm run build` 通过。」

## TikTok 创建抽屉 · 中抽屉尺寸

- [决策] TikTok 创建连接对齐 `drawer.design.md`：用 `SensDrawer` medium（864px），去掉 520px 自定义宽度。
- [实现] `CreateConnectionDrawer` `size="medium"`，移除 `CREATE_CONNECTION_DRAWER_WIDTH` 与 `style.width` 覆盖。
- [token] `SENS_DRAWER_WIDTH.medium`（864px）。
- [验收] ✅已验「`npm run build` 通过。」

## 管理页 · 标题栏与 Tab 零间距

- [决策] 对齐 Figma `4:5969`：Tab 区 `top: 72px` 紧贴 `SensPageTitleBar`，标题栏与 Tab 之间无 padding。
- [实现] `DataSourceManagementPage` 内容区去掉 `padding-top`，保留左右下 `spacing/6x`；Tab 与分区间仍 `spacing/4x`（16px）。
- [验收] ✅已验「`npm run build` 通过。」

## 阶段汇报 · 今日重心调整

- [决策] 明日阶段汇报与录屏演示优先：今日重心从「继续补齐基础组件 / token 体系」临时调整为 TikTok 数据源接入全流程可展示；Google Ads 不做完整页面，仅保留 DataSourceSpec 复用方向说明。
- [实现] TikTok 录屏路径相关代码批次见上文「方案 B 第一～三批 / 管理页·下钻页 Figma 还原 / 录屏数据 / 无侧导 / 抽屉尺寸」等节；基础样式与 Icon / Card / TitleBar / Drawer 样张并行补齐。
- [验收] ✅已验「`npm run build` 通过；TikTok feature 无 antd 直接 import。」
- [待做] ⏳待做「浏览器全路径录屏 10 步验收（管理页 → TikTok 下钻 → 创建 → 搜索 → 返回）。」

## SensPageTitleBar · Figma v2.1 落地页 / 下钻页（决策 · 待实现）

- [决策] 对齐 Figma `Sens.Design 标题栏 面包屑 v2.1`（fileKey `Q82i88qTxREh4lFwnbRLyb`）目录「一、类型」：`landing`（落地页 1.1，`white` 背景，72px 仅标题 / 84px 含辅助文案）与 `drilldown`（下钻页 1.2，`theme-title-background`，94px 含面包屑）分两类型；四种标准组合不拆组件，由 `description` + `actions` slot 覆盖：（1）标题+辅助文案+主按钮、（2）标题+主按钮、（3）面包屑+标题+返回+按钮、（4）面包屑+标题+返回+辅助按钮+按钮；图 3 / 4 差异仅在 `actions` 组合，不需新 variant。
- [决策] `variant` 命名采用 `landing` / `drilldown`（与 Figma 章节一一对应）；默认可推断：有 `breadcrumbItems` 或 `onBack` → `drilldown`，否则 → `landing`；业务页仍建议显式传入。
- [坑] 当前 `SensPageTitleBar` 写死 `theme-title-background` + 72px，管理页误用下钻页背景出现灰条；缺 `description`；下钻页返回按钮应在面包屑下方与标题同行（非面包屑左侧）；`title-bar.md` / `TitleBarShowcasePage` 未录入上述组合；**禁止**业务用 `style` 临时覆盖背景代替正式 variant。
- [坑] `title-bar.md` 现行规则「背景必须使用 `theme-title-background`」与 Figma 1.1 落地页冲突，实现 `variant` 后须改为按类型分支，并同步 `title-bar.design.md`。
- [坑] Figma 规范除「一、类型」外尚有目录二～六（标题与自定义区 / 返回 / 面包屑 / 辅助信息 / 操作区）及侧导+白内容区等多套页面 mock；代码与样张目前仅覆盖单一背景形态，规范覆盖面大于实现。
- [实现] 本日仅完成规范对齐与欠账登记，**未改** `SensPageTitleBar.tsx` / 业务页。
- [token] 落地页背景 `white`；下钻页背景 `theme-title-background`；辅助文案 `font-size/s` + `line-height/s` + `text-sub-color-transparent`；高度 72 / 84 / 94 待沉淀为 component 常量。
- [来源] TikTok 管理页 Figma `4:5963`：白内容区 `4:5967` 内标题栏 `4:1739` / `4:1740` 无独立底色（应对 landing）；下钻组合示例帧 `1661:94810`「面包屑+标题+主按钮」。
- [验收] ⏳待做「增 `variant` + `description`、修正下钻布局、TitleBar 样张四条 demo；修订 `title-bar.md` 背景规则；`DataSourceManagementPage` 改 `landing`（图 2），`DataSourceConnectionPage` 保持 `drilldown`（图 3）；浏览器对照 Figma。」

## 待办 · 未完成欠账

- Icon Foundation 后续：Button / Input / Select / Search 逐组件替换；antd 图标迁移单独推进。
- TikTok demo：浏览器全路径录屏验收；数据库等分区补全；顶导/侧导渐变 token 化；Meta 等 logo 占位替换；录屏前非一比一项（管理页标题栏灰底、渐变 hex）需在演示稿中说明或优先修高可见项。
- SensPageTitleBar：`landing` / `drilldown` 变体 + 四种标准组合录入样张；`title-bar.md` / `title-bar.design.md` 背景规则按 variant 修订（见上节）。

