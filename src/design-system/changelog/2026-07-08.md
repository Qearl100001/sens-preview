# 设计还原产线 · 更新日志 · 7月8日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## 标题栏 · 页面标题栏左右内距

- [决策] `SensPageTitleBar` 左右内距不对称：有返回时左 `spacing/0․5x`（2px）、右 `spacing/6x`（24px）；landing 无返回时左仍 `spacing/6x` 与页面内容区对齐。
- [实现] `SensPageTitleBar.tsx` 拆 `paddingInline` 为 `paddingLeft` / `paddingRight`；`TitleBarShowcasePage` 矩阵补左侧两行；`title-bar.md` 补落地规则。
- [token] `spacing/0․5x`、`spacing/6x`
- [验收] 🔶待验「`/components/title-bar` 下钻示例：返回图标距左 2px、右侧按钮距右 24px；landing 示例左 24px」

## 标题栏 · 有面包屑标题行对齐

- [决策] 仅 `SensPageTitleBar` 在「有面包屑、无 description」时走固定标题行布局：返回 icon / 一级标题 / 右侧按钮同在 `line-height/xxl`（30px）行内居中；不改抽屉 `SensTitleBar` 及其他 variant。
- [实现] `SensPageTitleBar.tsx` 增加 `useBreadcrumbTitleBandLayout` 分支；面包屑到标题 gap 改 `breadcrumbTitleGap`（2px）。
- [token] `line-height/xxl`、`line-height/s`、`spacing/0․5x`
- [验收] 🔶待验「`/components/title-bar`『渠道归因设置』示例：返回与标题垂直居中、按钮与标题行对齐」

## 标题栏 · 面包屑距顶 11px

- [决策] 有面包屑无辅助文案时，面包屑距顶与底部留白对称，各 `breadcrumbVerticalInset`（11px，由 72px 栏高减去面包屑/间距/标题行后平分推导，非独立 spacing token）。
- [实现] `SensPageTitleBar.tsx` · `useBreadcrumbTitleBandLayout` 容器加 `paddingTop: breadcrumbVerticalInset`；标题行中心距顶更新为 46px。
- [token] `size/component-height/title-bar`、`line-height/s`、`spacing/0․5x`、`line-height/xxl`
- [验收] 🔶待验「`/components/title-bar`『渠道归因设置』：面包屑距顶 11px、标题行下留白 11px」

## 标题栏 · 右侧操作距顶 20px

- [决策] 有面包屑无辅助文案时，右侧操作不与标题行 flex 垂直居中绑定，距标题栏顶固定 `spacing/5x`（20px），与 `SensTitleBar` 一致；仅返回 icon 与一级标题在标题行内居中。
- [实现] `SensPageTitleBar.tsx` · `useBreadcrumbTitleBandLayout` 拆左右列：左侧面包屑+标题行，右侧 `actionTop` 独立定位。
- [token] `spacing/5x`
- [验收] 🔶待验「`/components/title-bar`『渠道归因设置』：按钮距顶 20px，返回与标题仍同行居中」

## 标题栏 · 有面包屑 + 辅助文案（94px）

- [决策] 有面包屑且有 description 时并入同一固定布局：顶/底各 11px、面包屑→标题 2px、标题→辅助文案 4px、返回与标题同行居中（icon 顶 35px）、按钮距顶 20px；不走 grid 回退。
- [实现] `useBreadcrumbFixedLayout` 扩至全部 `showBreadcrumb`；新增 `breadcrumbVerticalInsetWithDescription`；description 块 `marginTop: spacing/1x`。
- [token] `size/component-height/title-bar-with-description`、`spacing/1x`、`line-height/s`
- [验收] 🔶待验「`/components/title-bar`『设备白名单』（有辅助文案）：间距与按钮距顶 20px」

## 标题栏 · 辅助文案与面包屑/标题左对齐

- [决策] 有面包屑时辅助文案与面包屑、一级标题从返回热区后同一条竖线起始，不贴左列顶边。
- [实现] `useBreadcrumbFixedLayout` · description 行补与面包屑行相同的返回占位 + `backContentGap`。
- [验收] 🔶待验「`/components/title-bar`『设备白名单』：辅助文案左缘与面包屑/标题对齐」

## Typography + Divider · 入库收口

- [决策] Typography / Divider 真相源在 `tokens/source/foundations/*.json` → `build-tokens.mjs` → `tokens.resolved.json`；helper 只保留 `getTypographyToken` / `getDividerColor` / `getDividerBorder`，删除全量 fallback 与运行时 spec 双份维护；`theme.ts` 内 antd 字号与 Input/Table 分割线改读生成 token，去除 14/12/22/18 硬编码。
- [实现] `build-tokens.mjs`、`typography.ts`、`divider.ts`、`DividerBasicStylePage.tsx`、`docs/foundations/typography.md`、`docs/foundations/divider.md`；重跑生成 `theme.ts`。
- [token] `font-size/m`、`line-height/m`、`divider/color/deep/transparent`、`divider/color/light/solid`
- [验收] ✅已验「`npm run build` 通过、`/basic-styles/typography` 样张」| 🔶待验「`/basic-styles/divider` 样张」

## 基础样式 · P0 token 绑定样张同步

- [实现] Typography / Shadow / Card 三个基础样式样张页与 `tokens.resolved.json` 对齐：Typography 层级与 Token 表改读 `getTypographyToken()`；Shadow 删除重复 layer spec、展示 `tokens.shadow` resolved 值；Card 描边/分割线统一 `getDividerColor()` / `getDividerHairlineWidth()`。
- [token] `font-size/*`、`line-height/*`、`shadow/D1/down`、`divider/color/outline/transparent`、`divider/color/light/transparent`、`divider/color/deep/transparent`
- [验收] ✅已验「`/basic-styles/typography`、`/basic-styles/shadow`、`/basic-styles/card` 目视与改前一致」

## 基础样式 · P1 getUnitToken + divider handle 映射

- [实现] 新增 `src/design-system/unit.ts`（`getUnitToken` / `normalizeUnitTokenName`）；`divider.ts` 导出 `DIVIDER_HANDLE_MAPPINGS` 与 `resolveDividerFromColorHandle`；`docs/foundations/divider.md` 补 handle 迁移对照表。
- [实现] 试点迁移：`SpacingBasicStylePage`、`SensPageTitleBar`、`SensTitleBar`、`SensBadge`、`SensSelectDropdown`、`SensDropdownMenu` 改读 `getUnitToken`；`SensSelectDropdown` 禁用边框改 `getDividerColor("light", "transparent")`。
- [token] `spacing/0.5x`、`spacing/vertical/2.5x`、`divider/color/light/transparent`
- [验收] ✅已验「`npm run build` 通过」| 🔶待验「标题栏 / 徽标 / 下拉浮层样张目视无回归」
