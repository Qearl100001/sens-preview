# 设计还原产线 · 更新日志 · 7月31日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## Tips · 常规便签 P0（Sens 自持）

- [决策] 基础组件录入「便签 Tips」：组件 `SensTips`、侧栏「便签」。仅**常规**便签（纯文案 + 箭头）；**高级便签**属业务组件，不进基础。承载选 **Sens 自持**浮层，不用 antd Tooltip 视觉。悬停 0.1s 出现、离开立即消失。
- [实现] `SensTips.tsx` / `tips.css`；`TipsShowcasePage`；`tips.md` / `tips.design.md`；`PreviewShell` / `App` 路由。
- [token] `tooltip-background`、`white`、`font-size/s`、`line-height/s`、`spacing/horizontal/2x`、`spacing/vertical/1x`、`radius/s`、`spacing/1x`；箭头边长 `SENS_TIPS_ARROW_SIZE=5`、最大宽 320、z-index 1060 为组件契约常量。
- [验收] ✅已验「Playwright：矩阵四向 open；bg=#3A3E46；12/18；pad 4/8；radius 3；maxWidth 320；悬停出现第 5 条、离开回 4」

## 横切 · 构建 chunk 拆分（全路由 lazy + vendor）

- [决策] 消除 Vite >500KB chunk 警告的主因：Showcase 全静态 import。路由页一律 `lazy`；antd/react/i18n 用 `manualChunks` 拆 vendor。不抬 `chunkSizeWarningLimit`。
- [实现] `App.tsx`（lazy + Outlet Suspense）；`vite.config.ts`（manualChunks）。
- [验收] ✅已验「`npm run build`：main ~57KB（原 ~911KB）；业务页按路由拆分；仅剩 `vendor-antd` ~1.0MB >500KB 警告（库本身）」· ✅已验「浏览器：`/components/button` 修复 LazyOutlet 透传 `outlet context` 后 0 error；此前白屏因 ShowcaseSelect 读不到 skin」

## 横切 · 上下文工程 P0（交接门禁）

- [决策] Handoff 日期落后 changelog >1 天则先补交接再改代码；三段式 changelog→Worklog→Handoff；Verified 升级须规则+Demo/矩阵+浏览器/自动化+交接证据链。Worklog 不与 Handoff/changelog 并文件。
- [实现] `AGENTS.md`；YuwenAI `Handoff.md` / `Decisions.md` / Worklog；README 组件表此前已补 tips/message/alert。
- [验收] ✅已验「Handoff 日期=changelog 最新日 07-31；AGENTS 含三段式；Decisions 含证据链」

## Tips · 矩阵跑偏修复 + Figma 软三角箭头

- [决策] 矩阵静态样张改 `strategy="anchored"`（相对触发源 absolute），避免右侧文档栏出现后 portal fixed 坐标不跟着布局左移而跑偏。箭头改 Figma `1172:218` 软肩 SVG path + `currentColor`，不落 raster。
- [实现] `SensTips.tsx` / `tips.css`；`tips.md` / `tips.design.md`。
- [token] 间距 `spacing/1x`；箭头常量 6/16；色 `tooltip-background`。
- [验收] ✅已验「浏览器 `/components/tips` 矩阵：四向间距 4px、中心对齐 d≈0；箭头 16×6 / 6×16；tip 在 cell 内 anchored」

## Tips · 最大宽 300 + 10.5 行内滚

- [决策] 对齐 Figma `6613:32568` / `6613:32687`：气泡最大宽 **300**（不足随文案变窄，横向排版）；最高 **10.5 × line-height/m**，超出内滚。滚动条拇指宽 6、圆角 `radius/s`、色 `tokenRgba(white, 0.8)`。最大宽落在 `.sens-tips-content`，不把箭头计入 300。
- [实现] `SensTips.tsx`（`SENS_TIPS_MAX_WIDTH=300`、`SENS_TIPS_MAX_LINES`、`SENS_TIPS_SCROLLBAR_SIZE`）；`tips.css`；`TipsShowcasePage`（短文 / 满宽 / 超高可滚）；`tips.md` / `tips.design.md`。
- [token] `line-height/m`、`radius/s`、`tokenRgba(white, 0.8)`；契约常量 300 / 10.5 / 6。
- [验收] ✅已验「Playwright `/components/tips`：短文宽 62（小于 300）；满宽气泡=300；超高 max-height=243（文字区 231=10.5×22）、可滚；writing-mode=horizontal-tb；scrollbar-color white 0.8」

## Tips · 12 向箭头对齐（placement × align）

- [决策] 补齐 Figma 组件效果 12 向：`align: start|center|end` 与 `placement` 组合为上左…右下。上下边槽 28（尖端距边 14）、左右边槽 22；尖端对准触发源中心。不做自动避让 / 宽触发源分段（仍后置）。
- [实现] `SensTips.tsx` / `tips.css`；矩阵 3 列 × 12 格；Demo 增加对齐选择；`tips.md` / `tips.design.md`。
- [token] 箭头契约常量 `SENS_TIPS_ARROW_EDGE_SLOT_*` / `INSET_*`；色/间距沿用既有 Tips token。
- [验收] ✅已验「Playwright：矩阵 12 标签齐全；上下边槽 28 / 左右边槽 22；尖端盒对准触发源中心 dx/dy≈0（交叉轴）」

## Tips · 左右边对齐距角 6 + Demo 对齐文案

- [决策] 左上/左下/右上/右下：三角距气泡顶/底 **6px**（`SENS_TIPS_ARROW_EDGE_GAP`），尖端中心距边 **14**，避免与圆角顶底连成一体。Demo 对齐选项随方向切换：上下→左中右，左右→上中下；不用「前/后」。
- [实现] `SensTips.tsx` / `tips.css`；`TipsShowcasePage`；`tips.md` / `tips.design.md`。
- [token] `SENS_TIPS_ARROW_EDGE_GAP=6`；`INSET_BLOCK=14`。
- [验收] ✅已验「Playwright：左上/右上 tip 距 content 顶=6、左下/右下距底=6、尖端中心距边=14、交叉轴 dy≈0；方向=右时对齐选项为上/中/下、无前/后」

## Tips · 常规 P0 全量验收

- [决策] 按 `tips.md` 验收规则对 `/components/tips` 做 Playwright 全量核对（矩阵 12 向 + 视觉 token + Demo 宽高滚动 + 悬停热区 + 对齐文案）。
- [验收] ✅已验「全部通过：矩阵 12；视觉 14/22、pad 10/6、radius 4、bg=#3A3E46、白字、maxW 300；间距 4；交叉轴对准；边槽 28/22；距角 6 / 尖端距边 14；短文 62 / 满宽 300 / 超高 243 可滚；延迟出现+热区保持+离开消失；对齐文案 左中右 / 上中下；0 page error」

## Tips · 消费方 antd Tooltip → SensTips

- [决策] 生产组件与相关样张统一改 `SensTips`；Table `showSorterTooltip`（antd 内置）本轮不动。
- [实现] `SensPagination` / `SensInput` / `SensTabs` / `TableShell` / `ProductShellSideNavigation` / `SensForm`；样张 `Checkbox` / `Radio` / `Button` FAB；`tips.md` / `tips.design.md`。
- [验收] ✅已验「`src/ui` 无 antd `<Tooltip`；Playwright：分页箭头 / 输入警告 icon / Checkbox 帮助均出 `.sens-tips-popup`」

## Tips · 自动避让 + 宽触发源分段对齐（P1）

- [决策] portal 默认 `autoAdjust`：先试传入 `placement`，装不下再按上→左→右→下；触发源宽 ≥300 按指针前/中/后 1/3 推导 `align`（无指针回退 prop）。矩阵 `anchored` 忽略避让。
- [实现] `SensTips.tsx`（`SENS_TIPS_WIDE_TRIGGER` / `SENS_TIPS_FLIP_ORDER`）；Demo 宽触发源样张；`tips.md` / `tips.design.md`。
- [验收] ✅已验「Playwright：宽触发源 320 悬左/中/右 → align start/center/end；贴顶触发（trTop=12）top 装不下 → 换 left」

## 搜索 · 验收轻收口（P0 + P1-1～4）

- [决策] 维持 antd 混合承载。P0：对外 Omit `disabled`（仅创建可有 `createDisabled`）；矩阵「激活选择器」走真实 `categorySelectOpen`。P1：长占位符截断 + tip（独立框 SensTips / Compact 内 native `title`）；Demo 补「简约·带分类」与高亮归属说明；`253` 明确无 spacing token。
- [实现] `SearchInput.tsx` / `MinimalSearchField.tsx` / `SearchPlaceholderTip.tsx`；`SearchShowcasePage`；`search.md` / `search.design.md`。
- [验收] ✅已验「Playwright：激活选择器格 `.ant-select-open`=2；长占位符悬停 SensTips 全文；Demo 含「简约 · 带分类」+ 高亮说明；矩阵 10 行」

## 搜索 · 分类浮层 Sens 化 + 创建链接色

- [决策] 分类下拉浮层复用 `sens-select-dropdown` + `useSensSelectDropdownStyle`（与 SensSelectDropdown 同源），不再用 antd 默认下拉皮。创建链接按钮默认 `text-color-transparent`（中性色/文字/01_主要），悬停/点击仍走 `link-color` / `link-active-color`。
- [实现] `SearchInput.tsx`（`useCategorySelectProps`）；`MinimalSearchField.tsx` / `search.css` / `searchTokens.ts`；`search.md`。
- [验收] ✅已验「Playwright：创建色 rgba(23,28,38,~0.9)=text-color-transparent；分类展开浮层 `.sens-select-dropdown` + 勾选图标」

## 搜索 · 清空图标悬停色

- [决策] 清空始终「浅灰圆+灰叉」（结构不变）；悬停只换色。Figma `3813:7332` 绑 `icon-sub-color-transparent`（03）；绿肤下 01/02 同 hex，换 `icon-article` 无可见差。「实心灰圆+白叉」非清空场景。
- [实现] `searchTokens.ts`（`--sens-search-clear-hover`）；`search.css`（仅 `color`，无 path fill 覆盖）；`search.md`。
- [验收] ✅已验「悬停 color=rgba(116,126,148,~0.88)=icon-sub；圆仍 0.3；叉非 white；结构未变」

## 搜索 · 验收 P1 文档收口

- [决策] 图标/主题色表统一写 Sens handle（去掉 `colorIcon` 等 antd 名混用）；清空「只换色」写入 `review-checklist` 专属规则；Demo 分类 / 简约·带分类挂长占位验收 Compact `native title`。
- [实现] `search.md`；`review-checklist.md`；`SearchShowcasePage.tsx`。
- [验收] ✅已验「文档表无 colorIcon；Showcase 分类变体传 LONG_PLACEHOLDER；checklist §7 含清空/无 disabled」

## 搜索 · Compact 占位 tip 改 SensTips

- [决策] 带分类 Compact 不再用原生 `title`；截断占位一律 `SensTips`（与独立框一致）。Compact 接缝靠 CSS 定宽外壳，不依赖 tipMode=native。
- [实现] `SearchPlaceholderTip.tsx`（移除 tipMode）；`SearchInput.tsx`；`search.css`；`search.md`；Showcase 文案。
- [验收] ✅已验「复合搜索长占位悬停 `.sens-tips-popup` 全文；非输入框原生 title」

## 横切 · 鼠标光标硬写收敛

- [决策] 按走查顺序：① `SensTag` 全改 `sensCursorValue`（帮助/警告仅 Tips → 恒 `default`）；② Tips Demo `var(--sens-cursor-default)`；③ 搜索输入 `--sens-cursor-text`、清空 `--sens-cursor-pointer`。
- [实现] `SensTag.tsx`；`tips.css`；`search.css`；`docs/foundations/cursor.md`。
- [验收] ✅已验「搜索输入 cursor=text、清空=pointer；CSS 规则含 --sens-cursor-*；src/ui 无硬写 pointer/default/not-allowed」

## Radius · 待补 3/4（散落硬写 + 卡片圆角来源）

- [决策] 本轮清非零游离圆角：Breadcrumb focus→`radius/s`；Badge 样张区 6/8→`radius/l`；EvalBadges 10/4→`xl`/`m`。卡片无独立 PDF，圆角来源写 Figma `1335:26192` → `radius/l`（`card.md` §4.3）。拼接 `0` 与真圆 `50%` 不改。
- [实现] `SensBreadcrumb.tsx` / `breadcrumb.css`；`SensBadge.tsx` / `badge.css`；`EvalBadges.tsx`；`docs/foundations/card.md`；`docs/foundations/radius.md` §9。
- [验收] ✅已验「rg 非零硬写：breadcrumb/badge 样张/EvalBadges 已无 2/6/8/10/4 字面量」

## 横切 · 换肤阶段 3（组件接线）

- [决策] 功能色消费统一 `functionalCssVar("--sens-skin-*", fallbackHandle)`：有 AppearanceProvider 跟肤，无 Provider 回落绿 handle。点击投影环用 `buildFunctionalActiveRingShadow()`。导航品牌态继续 `getNavigationColorToken`。链接 / 警告等状态色不动。
- [实现] `functional-skin.ts` helper；`SensButton` / Checkbox / Radio / Tabs / Pagination / Search / Input* / Select / FAB / Badge / SectionTitle / Icon；TikTok / AgentEval 壳；`previewTokens`；changelog / theme-skinning 待补勾选。
- [token] `--sens-skin-primary|hover|active|…`；`functionalSkin.*`；导航 `theme-*` handles。
- [验收] ✅已验「`tsc --noEmit`」· 🔶待验「浏览器预览切蓝：Button 一级 / Checkbox 选中 / Tabs / 顶侧导 / TikTok 壳变蓝；链接色不变」

## 横切 · 换肤阶段 2（Appearance Context）

- [决策] 预览「绿/蓝」默认同步 `FunctionalSkin` 与 `NavigationTheme`；规则上仍可独立，暂不暴露分控。antd 只被 `buildAntdThemeForSkin` 灌入 SensD 主色，不当色源。顶/侧导展示页与 `ProductShellSideNavigation` 读 Context，验证入口驱动。
- [实现] `appearance.tsx` / `appearance-antd.ts`；`App.tsx` Provider；`PreviewShell`；`previewOutletContext.ts`；换肤/导航色样张；`TopNavigationShowcasePage`；`SideNavigationShowcasePage`；`ProductShellSideNavigation`。
- [token] 消费 `functionalSkin.*`、`navigationTheme.*`、`getNavigationColorToken`；CSS 变量 `--sens-skin-*` / `--sens-nav-*`。
- [验收] ✅已验「`tsc --noEmit`」· 🔶待验「浏览器：预览设置切蓝后 theme-skinning / top-navigation / side-navigation 变蓝」

## 横切 · 换肤阶段 1（绿/蓝矩阵进源）

- [决策] 按换肤流程只做阶段 1：录入冰绽蓝导航 + 功能色 01–10，不动组件接线。绿功能色点击跟现有 handle（`#008C65`），不改跟绿表 `#008C64`。蓝顶导氛围复用绿叠层结构（蓝表未单列）。选中底 06–08 蓝表实色入 `functional-skin.json`（色板无同名阶）。
- [实现] `tokens/source/foundations/navigation-theme.json`（`blue` + accent/handles）；新增 `functional-skin.json`；`build-tokens.mjs` 生成 `functionalSkin`；`navigation-color.ts`（`NavigationTheme` 含 blue、`getNavigationAccent` / `getNavigationColorToken`）；`functional-skin.ts` 读生成矩阵；`navigation-color.md` / `theme-skinning.md` / `token-registry.md` / `color.md` §14。
- [token] `navigationTheme.green|blue`；`functionalSkin.green|blue`（primary…lightBackground）；品牌 handles 如 `theme-side-text-active` 蓝覆盖；功能色主阶 `基础色板/冰绽蓝/*`。
- [验收] ✅已验「`npm run tokens:build` + `tokens:check`；resolved 含 blue 顶导 `#3B78C9→#305BB0`、功能色蓝 primary=`冰绽蓝/10`、选中底 `#EDF2FC/#F5F8FF/#E6EDFA`」
