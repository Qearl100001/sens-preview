# 设计还原产线 · 更新日志 · 7月23日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## 带表格表单 · 灰底标题与表格缩进

- [决策] 灰底标题通栏拉满内容区、宽度自适应；仅表格外框内缩 `spacing/horizontal/4x`（16px），与标题文案左边对齐。不得把 16px 缩进加在标题+表格整块上。
- [实现] `form-templates.css`：去掉 `.form-templates-table-block` 的 `margin-inline`，改为 `.form-templates-table-block > .sens-table-shell`；同步 `composite/form.md`、`templates/form/table-form.md`。
- [token] `spacing/horizontal/4x`
- [验收] 🔶待验「浏览器打开 `/composite/form` → 带表格表单：属性配置灰条通栏；表格左右各内缩 16px，左边对齐灰条内标题文字」

## 灰底标题 · 到下方内容固定 16px

- [决策] 所有灰背景标题到下方同组内容的纵向间距固定为 `spacing/vertical/4x`（16px），不得用 8px（`2x`）或表单项 20px（`5x`）替代。
- [实现] `.form-templates-table-block` gap 改为 `content-gap`；新增 `form-templates-section-block` / `sens-form-section-block`；卡片表单与 Form 分组示意改为 section-block；白卡内灰条容器 gap 从 `7x` 改为 `4x`。同步 `form.md` / `title.md` / `form.design.md` / `table-form.md` / `card-form.md` / `composite/form.md`。
- [token] `spacing/vertical/4x`
- [验收] 🔶待验「`/composite/form` 带表格表单：属性配置灰条到底部表格外框 = 16px；卡片表单样式一灰条到字段区 = 16px」

## 设计系统索引 · 首轮收口

- [决策] `src/design-system/README.md` 作为组件规则的唯一索引；`.design.md` 为可选设计评审说明，不以是否成对判断组件是否缺失。`SensBreadcrumb` 暂归属 `title-bar.md`，独立使用场景出现后再拆分。
- [实现] 补齐 README 的基础 / 复合组件索引与阅读链；`ProductIndexPages.tsx` 对齐 Foundation、复合表格和 SDH 录入型表格的已收录状态；新增 Top Navigation 的设计和实现规则文档。
- [验收] 🔶待验「`/overview`、`/composite`、`/templates` 状态文案与对应文档一致；`/components/top-navigation` 可按新文档核对主导航、浮层与换肤」

## 规范与方法 · 站内阅读页

- [决策] 设计系统总览、AI 工作规则和 AI 设计方法维持并列入口：总览说明资产地图和阅读路径，规则定义执行与验收红线，方法定义从输入到交接的流程；三者相互链接但不复制原始 Markdown 全文。
- [实现] 新增 `/guides/design-system`、`/guides/agent-rules`、`/guides/methodology` 三个站内阅读页；`/guides` 的「设计系统交付」卡片接入对应路由。
- [验收] 🔶待验「依次打开三个 guides 页面：内容层级可扫读，卡片可跳转，设计系统总览可跳往工作规则与设计方法」

## 待办 · 未完成欠账（仅写在最新一天）

> 欠账已迁移至 [2026-07-24.md](./2026-07-24.md)。
