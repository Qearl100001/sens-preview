# 设计还原产线 · 更新日志 · 7月24日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## 设计系统治理 · 状态、文档与发布

- [决策] 不新增独立治理文档：`DESIGN.md` 定规则源与派生物关系，`src/design-system/README.md` 定状态模型与规则源文档模板，`changelog/README.md` 定团队发布标准。
- [实现] 统一成熟度（`Stable` / `Pilot` / `Planned` / `Deprecated`）、实现（`Implemented` / `Partial` / `Missing`）、验证（`Verified` / `Pending` / `Not Applicable`）三维状态；`P0` / `P1` 仅表示优先级。
- [决策] 下周团队交付定义为 `Sens.Design Design System v0.9`，发布类型为 `Current Coverage Release`；明确覆盖范围与不包含能力，避免将未稳定资产误认为团队默认方案。
- [验收] ✅已验「Markdown 结构与链接路径复核；`npm run build`」

## Foundation v0.9 · Color / Typography / Spacing 首轮迁移

- [决策] 三篇 Foundation 采用 `Stable · Implemented · Verified` 状态；Verified 仅代表规则文档和 Foundation 样张已验，不代表所有历史组件都已完成全量 token 化。
- [实现] Color 以 Token Source 的 `component-active` 为唯一绿色基线；导航主题从普通 Color Foundation 移回 `navigation-color.md`，链接明确为固定状态色，不参与 Functional Skin。
- [实现] Typography 明确 `typography.json → build-tokens.mjs → tokens.resolved.json` 生成链路；v0.9 以原子 token 与组合表为口径，不新增语义 helper。
- [实现] Spacing 将表单展开规则的唯一来源指向 Form 文档；Table 单元格 padding 标为 Table 组件待收口，不改实现或强行改规格。
- [实现] 修正 Input 框外 help 使用 `spacing/vertical/1x`，并区分 8px 字段 label 间距与 16px 灰底分组标题间距。
- [验收] ✅已验「`/basic-styles/color`、`/basic-styles/typography`、`/basic-styles/spacing` 的设计说明与数值样张；`npm run build`」

## 工程质量门 · Token 对照与源码 typecheck

- [实现] 新增 `docs/foundations/token-registry.md`：索引 token 分类、来源、生成物、语义维护源、换肤关系与 v0.9 录入状态；不复制或手改生成值。
- [实现] 新增 `npm run tokens:build` 与 `npm run tokens:check`。后者在临时目录重建 token，并逐字校验 `theme.ts`、`tokens.resolved.json` 与双语 i18n 生成物，不污染工作树。
- [实现] `npm run build` 现在先执行 token 一致性检查与独立 `npm run typecheck`，再执行 Vite build。
- [实现] `tsconfig.node.json` 已登记两份 Vite 配置，为后续 Node 类型检查准备覆盖范围。
- [验收] ✅已验「`npm run tokens:check`、`npm run typecheck`、`npm run build`」
- [待办] 🔶 ESLint / `@types/node` 依赖安装被当前 npm 镜像以 403 拒绝；恢复包访问权限后，再新增 lint 配置与 Vite 配置的独立 typecheck，不在本轮留下不可运行的半配置。

## 规则源治理 · 核心资产状态迁移

- [实现] Foundation 索引全面采用 `成熟度 · 实现 · 验证` 状态，替代新增的“初稿”等混合状态词。
- [实现] Layout、Theme Skinning、Button、Form、Table、Top Navigation、Composite Form、Composite Table 补齐定位、三维状态、来源与预览入口。
- [决策] 不将既有预览或历史描述自动视为验证完成；除已明确验收的 Color、Typography、Spacing 外，本轮核心资产如实标为 `Pending`。
- [验收] ✅已验「规则源头字段、索引状态与预览入口路径复核；`npm run build`」

## Foundation / Token 对照 · 其余资产收束

- [决策] Token 对照的 `Verified` 只表示生成物同步检查通过，不替代语义覆盖、组件消费和浏览器验收；因此本篇状态调整为 `Pilot · Partial · Pending`。
- [实现] Grid、Navigation Color、Size、Icon、Radius、Shadow、Card、Divider 统一迁移到三维状态头；Foundation 索引补入 Token 对照和 Divider，并如实标注 Shadow、Divider 已有生成 / helper 承接但待验，Card 尚无可复用基础组件。
- [实现] Token 对照补齐 Size、Radius、导航 handle 与导航主题、antd 组件映射、脚本层公式常量的分类边界；`tokens:check` 同时拒绝生成物中的 `UNRESOLVED(...)` 引用。
- [实现] 修正设计系统索引的 `build-tokens.mjs` 路径和变更顺序：先改 `tokens/source/`，再按需调整映射并重建、校验；Foundation 状态看板同步 Navigation Color、Shadow、Divider 的当前缺口。
- [验收] ✅已验「`npm run build`（含 token 同步、类型检查与 Vite 构建）；浏览器打开 `/basic-styles/foundation-status`，核对状态看板可渲染」

## 规则源治理 · 单一真相源裁决

- [决策] 同一规则可被索引、引用或验证，但只有一个可展开维护源；数值以 `tokens/source/` 为准，通用规则以 Foundation 正文为准，组件与样板间边界以各自 `.md` 正文为准。
- [实现] `DESIGN.md` 新增来源对照与冲突裁决顺序；设计系统与 Foundation 索引明确自身只承担索引、状态汇总或链接，不复制规则细节。
- [实现] Foundation / Token 状态看板明确为人工维护的派生辅助视图；“高 / 中”等推进判断不替代规则源的三维状态，发生不一致时回写看板。
- [验收] ✅已验「`npm run build`；最新 Vite 实例打开 `/basic-styles/foundation-status`，辅助说明文案已渲染」
- [待办] ⏳ 看板仍是手工摘要，尚未自动从 Markdown 规则源生成；不在本轮引入解析或构建链。

## Token 直读迁移 · 第一期（antd runtime token 摘除）

- [决策] 语义色一律从 `tokens.resolved.json` 经 `getColorToken` / `tokenRgba` / `getDividerColor` 直读，不再经 `theme.useToken()` 读 antd runtime token；本期只做换源不换值，antd 布局组件（如 Space）不在摘除范围。
- [实现] `StatusBadge`：圆点色直读 `success-color` / `warning-color` / `text-color-disable`，文字直读 `text-color`，移除 antd theme 依赖。
- [实现] `useSensIconTokens`：图标角色直读 `icon-color-transparent` / `icon-sub-color-transparent` / `icon-color-transparent-disable` / `text-color` / `component-primary` / `white`；secondary 与 disabled 从 antd 兜底值切换为 icons.md 定义的透明 handle。
- [实现] `searchTokens`：主色链直读 `component-primary` / `component-hover` / `component-active`，链接链直读 `link-color` / `link-active-color`，字段边框改 `getDividerColor("deep", "transparent")`，分区分割线改 `getDividerColor("light", "solid")`，radius / control-height 直读 `radius/m` 与 `size/component-height/m`；移除对 `buildAntdTheme()` 组件映射的反查，返回值不再暴露 antd token 对象。
- [实现] `SearchHighlight`：高亮色改 `getColorToken("component-primary")`。
- [实现] `text-color-chains`：`getLinkTextChain` / `getWarningTextChain` 改为无参，直读 `link-color` / `warning-color` 三态链；`SensDropdownMenu` 调用同步更新。
- [验收] ✅已验「`npm run build`；浏览器 `/components/table` 读取 StatusBadge 实际渲染色（正常 #5CB838 / 停止 #B3B7BE / 失败 #E54545，文字 #2F333C）；下拉菜单 `--sens-text-link*` / `--sens-text-warning*` 变量值与 token 链一致；`/components/select-dropdown` 高亮实测 #00B280（component-primary）；搜索页聚焦态绿边框 + 光晕正常，控制台无 divider 缺失警告」
- [待办] 🔶 第二期起：`SensDropdownMenu` 剩余 `theme.useToken()` 读值（bg / radius / disabled 色）、SensInput / SensSelect / Tabs 等仍经 antd theme 桥接的组件逐步直读。

## Token 直读迁移 · 第二期 2a（SensDropdownMenu 摘除 theme.useToken）

- [决策] `SensDropdownMenu` 语义色与圆角全部直读 design token；antd 不再作为本组件视觉源。禁用色从 antd 算法值 `rgba(0,0,0,0.25)` 收敛为语义 handle `text-color-disable`（与 StatusBadge / Button 禁用文字同源）。
- [实现] `useSensDropdownMenuStyle`：`colorBgContainer` → `white`；`borderRadius` → `radius/m`；`colorTextDisabled` → `text-color-disable`；移除 `theme` / `theme.useToken()` 导入与调用。
- [验收] ✅已验「`npm run build`；重启 Vite 后 `/components/select-dropdown` 矩阵：bg `#FFFFFF`、radius `4px`、disabled `#B3B7BE`（渲染 `rgb(179,183,190)`）；`/components/table` 更多菜单 portal 变量同源」
- [待办] 🔶 2b：`SensSelectDropdown` 触发框 / 浮层 / 预览矩阵仍读 antd token；2c：`SensTabs` 预览 fallback。

## Token 直读迁移 · 第二期 2b（SensSelectDropdown 摘除 theme.useToken）

- [决策] Select 触发框与浮层视觉变量一律直读 design token；antd Select 仅保留结构宿主。
- [实现] `useSensSelectTriggerStyle`：`colorPrimary/Active` → `component-primary/active`；`colorErrorHover/Active` → `warning-color-hover/active`；`colorIconHover` → `text-color`。
- [实现] `useSensSelectDropdownStyle`：`borderRadius` → `radius/m`。
- [实现] `getSelectTriggerPreviewStyleToken` 改无参直读；`SelectTriggerStatesPreview` 移除 `theme.useToken()`；文件内不再导入 antd `theme`。
- [验收] ✅已验「`npm run build`；`/components/select` 触发框变量 hover `#00b280` / active `#008C65` / errHover `#EB6767` / errActive `#B22B2B` / iconHover `#2F333C`；预览矩阵同源；`/components/select-dropdown` popup radius `4px`，高亮仍为 `rgb(0,178,128)`」
- [待办] 🔶 2c：`SensTabs` 预览矩阵 fallback 仍读 antd token。

## Token 直读迁移 · 第二期 2c（SensTabs 摘除 theme.useToken）

- [决策] Tabs 预览矩阵不再读 antd runtime token；优先沿用 `buildAntdTheme().components.Segmented` 静态映射（同源 `tokens/source`），缺失时直读 design token handle。
- [实现] `getPreviewStyleToken` 改无参；`colorPrimary/Hover/Active` fallback → `component-primary/hover/active`；`colorIconHover` → `text-color`；选中投影 fallback 改 `buildShadowD1()`；`TabsStatesPreview` 移除 `theme.useToken()`。
- [验收] ✅已验「`npm run build`；`/components/tabs` 矩阵：选中字色 `rgb(0,178,128)`，点击态 `rgb(0,140,101)`，禁用 `rgba(0,13,38,0.3)`，删除图标 hover 变量 `#2F333C`」
- [待办] ✅已在第三期去掉 `buildAntdTheme().components` 反查；antd Tabs / Segmented 仍作结构宿主。

## Token 直读迁移 · Preview 层摘除 theme.useToken

- [决策] Preview / DesignSystemDoc / Showcase 布局不再运行时读 antd ThemeToken；新增 `getPreviewTokens()` 用同名属性桥接 design token，保持与 `theme.ts` seed 视觉等价。`App.tsx` / `buildAntdTheme` 注入链路不动。
- [实现] 新增 `src/preview/previewTokens.ts`：色值经 `getColorToken`，间距 / 圆角 / 控件高经 `getUnitToken`，字号 / 字重经 `getTypographyToken`；`borderRadiusLG=8`、`lineWidth=1`、`fontFamilyCode` 按 antd 派生 / 字面量对齐。
- [实现] PreviewShell、ComponentShowcaseLayout、ComponentDocPanel、DesignSystemDoc 及全部相关 showcase / basic-styles 页面：`theme.useToken()` → `getPreviewTokens()`；移除 antd `theme` 导入。
- [token] `component-primary` / `link-color` / `text-color` / `text-sub-color` / `text-color-disable` / `outline-color` / `background-01` / `white` / `background-grey` / `background-04` / `background-grey-hover` / `component-light-background`；`radius/m|s`；`size/component-height/m|xl`；`spacing/1x…6x`；`font-size/s|m|l`；`font-weight/semibold`。
- [坑] `radius/l=6` 与 antd 派生 `borderRadiusLG=8` 不等价，本轮刻意用 8 保视觉；`colorFillTertiary/Quaternary` 取最近 `background-*`（非 seed，近似派生 fill）。
- [验收] ✅已验「`rg theme.useToken|useToken()` → `src/preview` 零命中；`npm run build`（tokens:check + typecheck + vite）」
- [验收] 🔶待验「浏览器抽样 PreviewShell / basic-styles 色距与改前对照」

## Token 直读迁移 · 第三期（清干净 runtime antd token）

- [决策] `src/ui` + `src/preview` 一律禁止 `theme.useToken()`；视觉源只认 design token。`buildAntdTheme` + `ConfigProvider` 仅作 antd 宿主注入桥，不作为业务读值源。
- [实现] `SensBadge`：`component-primary` + `font-size/s`；`SelectDropdownEmpty`：`link-color`；`SensFabGroup`：删除无参 `antdToken` / `useToken`。
- [实现] `SensTabs`：去掉 `buildAntdTheme().components.Segmented` 静态反查；预览矩阵与 pill 结构变量直读 `component-*` / `tokenRgba(...)` / `radius/*` / `spacing/*` / `font-size|line-height/*` / `buildShadowD1()`（与 build-tokens Segmented 映射同源）。
- [实现] Preview 层见上一节 `getPreviewTokens()`。
- [验收] ✅已验「`rg theme.useToken|useToken()` → `src/**/*.{ts,tsx}` 零命中；`npm run build`」
- [验收] 🔶待验「浏览器抽样 `/components/badge`、`/components/tabs`、`/components/select-dropdown`、PreviewShell」

## 走查回修 · 胶囊 Tab / 菜单禁用 token

- [决策] 胶囊态色对齐 Figma handle，不再对已带 alpha 的透明文字 handle 二次 `tokenRgba(..., 0.3)`；选中底用 `white`；选中悬停用 `component-hover`（antd Segmented 无独立 token，CSS 补齐）。
- [实现] `SensTabs` 预览矩阵：默认 `text-sub-color-transparent`；禁用 `text-color-transparent-disable`；悬停 `component-primary`；点击 `component-active`；选中 `component-primary`；选中悬停 `component-hover`。
- [实现] `tabs.css`：小/大胶囊 label `inline-flex` 水平垂直居中；去掉原先 `padding-block: 3px` 造成的偏上。
- [实现] `build-tokens.mjs` Segmented：`itemColor` / `badgeText` → `text-sub-color-transparent`；禁用链直读透明禁用 handle；重建 `theme.ts`。
- [实现] `SensDropdownMenu` / `SensSelectDropdown` 禁用字：`text-color-disable` / 二次 alpha → `text-color-transparent-disable`（对齐 Figma `17623:59746` `rgba(0,13,38,0.3)`）。
- [token] `white` / `text-sub-color-transparent` / `text-color-transparent-disable` / `text-color-transparent-disable-hover` / `component-primary` / `component-hover` / `component-active`
- [验收] ✅已验「`npm run tokens:build` + `npm run build`」
- [验收] 🔶待验「浏览器 `/components/tabs` 小胶囊居中与态色；`/components/select-dropdown` 禁用菜单项」

## 走查回修 · 基础标签页悬停/点击/选中字重 + 内容间距（Figma 3695 / 3713）

- [决策] 基础 line Tabs：悬停 `component-primary`、点击 `component-active`、选中 `component-primary` + `font-weight/semibold`(600)；内容距 nav `spacing/4x`(16)。
- [实现] `build-tokens.mjs` Tabs：`itemHoverColor`←`component-primary`；`itemActiveColor`←`component-active`；重建 `theme.ts`。
- [实现] `.sens-basic-tabs`：选中字重 600；nav→content `spacing/4x`；悬停/点击徽标切浅底功能色；`barOnly` 不加 16 间距。
- [token] `component-primary` · `component-active` · `component-light-background` · `font-weight/semibold` · `spacing/4x`
- [验收] ✅已验「Playwright：悬停 #00B280 · 点击 #008C65 · 选中字重 600 · nav→content gap 16」

## 走查回修 · 页签标签页 SensD 对齐（Figma 6354 / 4205）

- [决策] 页签不用 antd 默认灰影 / 默认 padding / 默认关闭色；结构与六态全部直读 SensD token。
- [实现] `SensEditableCardTabs`：`padding` 左右 `spacing/3x`(12) / 上下 `spacing/1․5x`(6)；高 `size/component-height/m`；顶圆角 `radius/m`；未选底 `background-transparent-grey`；选中白底 + `outline-color-transparent` 边；字色默认 `text-sub-color-transparent` / 悬停·选中 `component-primary` / 选中悬停 `component-hover`；关闭默认 `icon-color-transparent`、悬停 `warning-color`；未选默认关闭 opacity 0。
- [实现] 矩阵预览关闭悬停由错误的 `text-color` 改为 `warning-color`。
- [实现] Figma `4420:15544` 整框：内容区白底 + `outline-color-transparent`；末尾加号 `editor-add`（非「添加页签」文案），色链对齐 **图标弱化链接按钮**（`text-sub-color` / 悬停 `link-hover-color` / 点击 `link-active-color`），间距 `spacing/2x`(8)；antd `nav-add` 宿主，不嵌套 `SensButton`。
- [实现] Figma `6354:27070` 编辑前：标题选区 `component-primary` 底 + `white` 字；矩阵 `beforeEdit` 同步。
- [实现] Figma `6354:27099` 编辑中：无边框 input，字色 `text-article-color`，可真实输入；Demo 双击标题进入，加号新建后自动进入编辑；Enter/失焦提交、Esc 取消。
- [token] `spacing/3x` · `spacing/1․5x` · `size/component-height/m` · `radius/m` · `spacing/1x` · `spacing/2x` · `size/icon/m` · `font-size/m` · `line-height/m` · `white` · `background-transparent-grey` · `outline-color-transparent` · `text-sub-color-transparent` · `text-sub-color` · `text-article-color` · `component-primary` · `component-hover` · `icon-color-transparent` · `warning-color` · `link-hover-color` · `link-active-color`
- [实现] 页签缝合：选中底边无分隔线（白底 + `::after` 遮挡）；`content-holder` 去顶边，避免与 `nav::before` 叠成双线；内容区按 Card Foundation：`spacing/4x` padding · `radius/l` · `text-color-transparent` 正文。
- [实现] 加号点击后 blur + `:focus` 不再残留链接蓝（悬停蓝仍为 SensD `link-hover-color`，弱化链接色链）。
- [实现] 溢出「更多」：Figma `804:78`/`804:79`（`chevron-down`/`chevron-up` 实心三角）；仅 click 展开；展开态 `component-primary` + 向上三角；浮层复用 `useSensDropdownMenuStyle` + `.sens-card-tabs-dropdown`；激活/增删用 document mousedown 点外关闭（不走 visible 受控）。
- [实现] 加号仅新增并激活新页签，**不**自动进入编辑；编辑仅双击标题。
- [实现] 编辑态锁标题槽宽（进入前 `getBoundingClientRect`），页签不因 input 变宽；关闭 icon 改 `SensIcon name="close"` + `size/icon/m`(16)。
- [验收] ✅已验「Playwright：编辑前后页签宽 88→88；关闭 svg 16×16」
- [验收] ✅已验「CDP：默认灰向下；展开绿向上；切页签后 aria-expanded=false、图标回灰向下」
- [决策] 页签标签页归属基础组件；编辑/拖拽细则写入 `tabs.design.md`；拖拽排序仍为实现 TODO。
- [实现] 文档：`tabs.design.md` 补「编辑 / 拖拽」（双击编辑、失焦保存、过长 tooltip、半宽换位）；`tabs.md` 同步边界与实现映射。
- [实现] `SensEditableCardTabs` Pointer 拖拽排序：悬停提示时序；按下超阈值后选中并关 tooltip；X 位移超邻页半宽换位；关闭/加号/更多不参与。
- [验收] 🔶待验「悬停文案；按下即选中；半宽换位；双击编辑仍可用」
- [实现] 溢出更多下拉：自建全量 `Menu` overlay（无删除）；`visible` 受控 + `getPopupContainer=body` + `bottomLeft`；箭头态跟 `--more-open`。

## 优先待验收口 · `/composite/form` · `/composite/table` · `/guides/*`

- [决策] 本轮只做浏览器视觉与几何验收，不改实现；验证状态只覆盖已实现范围，不把 Partial 缺口写成 Verified 完成。
- [验收] ✅已验「`/composite/form` 带表格表单：灰底标题通栏；表格相对标题容器内缩 16px；表格左边与标题文案左边对齐（delta 0）；标题到表格纵向 16px；卡片表单灰条到内容 16px，字段 label 与标题文案左对齐」
- [验收] ✅已验「`/composite/table`：筛选区 title↔控件 8px、项间距 / 流 gap 16px；`SensTableFilterBar` + `TableShell` + Pagination 同页渲染；录入型单元格高 56、padding 12×8」
- [验收] ✅已验「`/guides` 三张交付卡片可跳转；`/guides/design-system`、`/guides/agent-rules`、`/guides/methodology` 均正常渲染正文」
- [实现] `composite/form.md` → `Verified`；`composite/table.md` → `Verified`（实现仍为 Partial）；复合组件索引卡片状态同步。

## SensD 鼠标态（Cursor Foundation）· 页签拖拽改 move

- [决策] SensD 鼠标操作五组落地为位图 + CSS 变量 / 类名；**页签拖拽**用「移动 `move`」（Figma `1145:160` 四向箭头），不用系统或 SensD `grab` / `grabbing`。
- [实现] 资源 `public/cursors/*.png`（含 default / pointer / not-allowed / text / vertical-text / crosshair / move / no-drop / grab / grabbing / copy / resize 族 / col-resize）。
- [实现] `src/ui/cursors.css`（`--sens-cursor-*` + `.sens-cursor-*`）；`src/design-system/cursors.ts` registry；规则 `docs/foundations/cursor.md`；索引挂到 foundations / design-system README。
- [实现] `SensEditableCardTabs`：`tabs.css` 悬停可拖与 `--dragging` 均 `var(--sens-cursor-move)`；`tabs.md` 同步。
- [坑] 父组导出含白底卡片，已抠透明；hotspot 为估算；`not-allowed` 与 `no-drop` 当前导出视觉几乎相同。
- [验收] 🔶待验「浏览器：页签悬停 / 拖中为四向 move；自定义 url 在目标浏览器生效」

## SensD 鼠标态 · basic-styles 走查页

- [决策] 鼠标态验收做成 Foundation 预览页，不另开组件 showcase；hover 热区切自定义光标，静态图仅作辅助。
- [实现] `/basic-styles/cursor`：`CursorBasicStylePage` 按 Figma 五组（默认 / 状态 / 选择 / 移动 / 调整尺寸）展示全部 `SENS_CURSORS`；`move` 标「页签拖拽使用」；侧栏「鼠标」入口；`cursor.md` 补预览链接。
- [验收] 🔶待验「浏览器打开 `/basic-styles/cursor`，逐组 hover 确认自定义光标；对照 `/components/tabs` 拖拽 move」

## SensD 鼠标态 · 20×20 主验 8 态 + 规则文案

- [决策] 用户确认位图画布 **20×20**（非 40/48）；主验 8 节点精导；规则文案取自《鼠标指针 v2.1》并统一「标示」。
- [实现] 重导 `1123:157/160/163/161/164`、`1145:160`、`1123:155/171` → 居中贴入 `public/cursors/*.png`（20×20）；其余态由旧图缩至 20×20（待精导）。
- [实现] `cursors.css` / `cursors.ts` hotspot 改相对 20×20；registry 补 `labelZh` / `description` / `primary`；`cursor.md` 与 `/basic-styles/cursor` 分主验 / 次要，卡片展示规则描述。
- [验收] ✅已验「8 主验 PNG `file` = 20×20」
- [验收] 🔶待验「浏览器 hover 尺寸观感；页签拖拽 move；次要态精导」

## SensD 鼠标态 · 改回系统光标（规则仍用 SensD）

- [决策] 真实指针用 **系统 `cursor` 关键字**（原生、可直接用）；SensD 只约束语义（何时 move / grab 等）。不再 `cursor: url(png)`，避免贴图感与粗描边。
- [实现] `cursors.css` 变量改为关键字；`cursors.ts` 以 `cssValue` 为准，PNG 降为 `illustration`；走查页示意改回 32px，hover 验收系统光标；`cursor.md` / `tabs.md` / README 同步。
- [token] 无色值 token；消费 `--sens-cursor-move` 等（= `move`）
- [验收] 🔶待验「`/basic-styles/cursor` hover 为系统指针；`/components/tabs` 拖拽为系统 move」

## 待办 · 未完成欠账（仅写在最新一天）

- SensD 鼠标态：浏览器验收 `/basic-styles/cursor` 系统光标切换；页签拖拽系统 `move`。
- 走查回修：浏览器复核 `/components/tabs` 小胶囊居中与态色；`/components/select-dropdown` 禁用菜单项色。
- Token 直读第三期：浏览器抽样 `/components/badge`、PreviewShell / basic-styles 色距对照。
- 筛选表格：列设置面板、复杂排序、筛选结果 ↔ `foundTotal` / 分页真实闭环。
- 录入型表格：校验、真实增删与滚动规则；树 / 嵌套交叉表格仍待收录。
- 表格 loading GIF。
- Button、Form、Table、Top Navigation 等基础组件的 Pending 浏览器验收与状态矩阵仍待逐项完成。
- Navigation Color 的其他 Product Shell Theme 矩阵、全局 Functional Skin、Card 基础组件与 Foundation 语义覆盖验收仍待后续推进。
- Preview `getPreviewTokens` 属性名仍沿用 antd ThemeToken 命名（值已是 design token）；后续可按需改成 Sens 命名，非阻塞。
- `buildAntdTheme` 仍注入 antd 宿主；业务代码已不再 runtime 读 antd token。
