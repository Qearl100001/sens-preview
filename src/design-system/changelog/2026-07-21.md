# 设计还原产线 · 更新日志 · 7月21日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## 复合表格 · 筛选区（SensTableFilterBar）

- [决策] 筛选区挂复合表格，不定为基础组件；128/148 定宽与 112 展开可视高用组件常量，不新录入 unit token。
- [实现] 新增 `SensTableFilterBar` + `table-filter.css`：左标题右控件（`spacing/horizontal/2x`）、项/行距 16（`spacing/*4x`）、展开/收起链接按钮、展开区 `max-height: 112` 滚动；`/composite/table` 接筛选 + TableShell + Pagination 样张；更新 `composite/table.md`。
- [token] 间距走 `spacing/horizontal/2x` · `spacing/horizontal/4x` · `spacing/vertical/4x`；标题 `text-color-transparent` @0.9 + `font-size/m` + `font-weight/medium`；定宽常量 `TABLE_FILTER_TRIGGER_WIDTH` / `TABLE_FILTER_EXPANDED_MAX_HEIGHT`。
- [验收] 🔶待验「浏览器打开 `/composite/table`：收起仅首行；展开后更多区滚动且约 2.5 行可视；空态触发 128、选长文案约 148；右侧展开/收起切换」

## 待办 · 未完成欠账（仅写在最新一天）

> 欠账已迁移至 [2026-07-23.md](./2026-07-23.md)。
