# 设计还原产线 · 更新日志 · 6月25日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## 动作下拉菜单 · 共享字色链（P1 + P2）

- [决策] 菜单行 = 浮层选项行本族实现，不嵌 `SensButton`；link/danger 字色与按钮共用语义 token，但经共享模块注入，避免各组件手抄 alias。
- [实现] 新建 `src/design-system/text-color-chains.ts`（`getLinkTextChain` / `getWarningTextChain` / `toTextChainCssVars` + 菜单别名 helper）；`useSensDropdownMenuStyle` 改引 `--sens-text-link*` / `--sens-text-warning*`，菜单 scoped 变量改为 `var(...)` 别名。按钮侧未改。
- [token] 链接链 `colorLink` / `colorLinkHover` / `colorLinkActive`（`link-color` 三态）；警告链 `colorError` / `colorErrorHover` / `colorErrorActive`（`warning-color` 三态）；行底仍 `background-transparent-grey-hover` @6% / `background-01-transparent` @8%。
- [验收] ✅已验「真实 portal · `http://127.0.0.1:5182/components/select-dropdown` · 更多 ▼ · link 行 default/hover/active 字色与 P2 前一致；行底 hover/active 仍为 `rgba(0,21,64,0.06/0.08)`；`.ant-select-dropdown` 未注入 `--sens-text-link`；按钮 link 矩阵未动。」

## 动作下拉菜单 · Demo 禁用/加载 + 光标（真实 portal）

- [决策] 真实「更多」浮层须有可验的 `disabled` / `loading` 真项；光标规则挂在 `.sens-dropdown-menu` 后代（portaled 与静态面板同源），不靠矩阵静态类。
- [实现] `SelectDropdownShowcasePage` / `ButtonShowcasePage` demo 改为 `MORE_MENU_DEMO_ITEMS`（编辑/复制/删除 + 归档 `disabled:true` + 同步 `loading:true`）；`dropdown-menu.css` 收敛可用行 `pointer`、禁用/加载 `not-allowed`（行+子节点）。
- [坑] 先前光标「跑了没反应」主因：① 真实浮层无 `loading` 项；② 若项仅视觉灰而未挂 `--disabled`，基类 `cursor:pointer` 不会变；矩阵静态壳与 portal DOM 不是同一验法。
- [验收] ✅已验「`http://127.0.0.1:5190` 真实 portal：编辑 hover `pointer`+灰底；归档/加载 hover `not-allowed`、点击不关菜单；加载转圈+加载中+置灰；`--sens-text-link` / 行底灰未回退；Select 未污染。」

## 下拉链接按钮 · 触发 click 锁死

- [决策] 「更多 ▼」动作菜单须 **点击展开**（▲ open），不用 antd `Dropdown` 默认 `hover`；按钮页与下拉浮层页共用 `SensDropdownButton`，一处改即对齐。
- [实现] `SensDropdownButton` 默认 `trigger={['click']}`；`dropdownProps.trigger` 可覆盖。
- [验收] ✅已验「hover 不展开、click 展开；button / select-dropdown 两页 demo 一致。」

## 预览工程 · 单端口 + 顶栏端口提示

- [决策] 多开 Vite 会静默换端口（5174/5175…），导致按钮页仍见旧 demo（查看详情等）；固定 `5173 --strictPort` 并在顶栏显示当前端口。
- [实现] `package.json` `dev` 脚本；`PreviewShell` `DevPortHint`；两页真实「更多」demo 加 `data-sens-demo="dropdown-more"`（与矩阵静态「更多」样张区分）；按钮页辅助文案对齐 `MORE_MENU_DEMO_ITEMS`。
- [验收] ✅已验「`:5175`/`:5195` 按钮 demo 浮层为 编辑/复制/删除/归档/加载中；`:5173` 若为旧进程则须先停掉再 `npm run dev`。」

## 动作下拉菜单 · 两场景 Demo + SensButtonActionMenu（方案 A）

- [决策] 触发器（`SensButton` / `SensDropdownButton`）与菜单行（`SensDropdownMenuItem`）分离；新增薄封装 `SensButtonActionMenu` 供一/二级按钮 + 可配 `trigger`，**本轮不改** `SensDropdownButton` 内部（收口薄封装为单独一轮）。
- [实现] 新建 `SensButtonActionMenu.tsx`（antd `Dropdown` + `SensDropdownMenu` / `SensDropdownMenuItem` + `useSensDropdownMenuStyle` + `overlayClassName="sens-dropdown-menu-overlay"`）；`DropdownMenuUsageScenarios.tsx`（`DROPDOWN_MENU_USAGE_SCENARIOS` 数组 + `ready|pending` + map）；`SelectDropdownShowcasePage` 移除静态「链接菜单面板」，改挂场景容器；`ui/index.ts` export。未动 `SensDropdownMenu` / `SensDropdownMenuItem` / `dropdown-menu.css` / `text-color-chains` / 矩阵。
- [验收] ✅已验「`http://127.0.0.1:5173/components/select-dropdown` · 场景① `[data-sens-demo="dropdown-more"]` hover 不展开、click 展开 link 项（含归档禁用/同步加载中）；场景② primary/secondary 真 portal（`sens-dropdown-menu-overlay` + `--sens-dropdown-menu-bg` token，不在 `.ant-select-dropdown` 内）、default 项、点项 `message.info('已跳转：…')` 且浮层 `display:none` 收起；静态「链接菜单面板」已移除；加场景 = push `DROPDOWN_MENU_USAGE_SCENARIOS`。」

## 动作下拉菜单 · 场景②触发器改为「更多 ···」真身

- [决策] 场景②（悬停菜单）触发器须为矩阵同款「更多 ···」（二级 + `MoreIcon` 尾标），不用通用 primary/secondary「按钮」占位；抽出 `SensMoreButton` 供运行时与矩阵共用。
- [实现] 导出 `SensMoreButton`（`SensButton.tsx`）；`SensButtonActionMenu` 在 `children` 为元素时直传触发器；`DropdownMenuUsageScenarios` 场景②改为单颗 `SensMoreButton` + `trigger={['hover']}`。未动场景① / `SensDropdownButton` / 菜单栈 / scenario 数组结构。
- [验收] ✅已验「`http://127.0.0.1:5173/components/select-dropdown` · `[data-sens-demo="dropdown-more-ellipsis-hover"]` 为 `SensMoreButton`（import `../ui` → `SensButton.tsx`）；hover/click 开 default 浮层、点项 message + 收起；场景①与菜单组件零回退。」

## 按钮 · 矩阵「更多 ···」与 SensMoreButton 单一来源

- [决策] `SensMoreButton` 为运行时真身；矩阵「更多 / 大·小尺寸」须走同一组件，不再在 `PreviewEntry` 手配 `MoreIcon` + antd `Button` 平行实现。
- [实现] `ButtonStatesPreview` · `PreviewRow` 在 `entry.key === "more"` 时改渲染 `SensMoreButton`（snapshot 样式 + `pointer-events: none` 保持静态格）；`baseEntries` more 行去掉冗余 `icon`/`iconPosition`。未动场景① / 菜单栈 / `SensDropdownButton`。
- [验收] ✅已验「全仓仅 `SensButton.tsx` 一处 `SensMoreButton` 定义；矩阵与场景② demo 均 import/调用同一组件。」

## 选择器 / 下拉浮层 · 合体文档拆为四文件

- [决策] 原 `select-dropdown.*` 按官方稿拆为浮层面板（`dropdown-menu.*`）与选择器触发框（`select.*`）；灰色段交叉引用、不双份正文。
- [实现] 新建 `dropdown-menu.design.md` / `dropdown-menu.md`（R1+R2）、`select.design.md` / `select.md`（R3）；`SelectDropdownShowcasePage` → `dropdown-menu.*`，`SelectShowcasePage` → `select.*`；旧 `select-dropdown.*` 改 stub。未补动作菜单专节 / 级联等现稿无章节。
- [验收] ✅已验「`select.md` / `select.design.md` 无浮层容器/六面正文，仅引用句；`dropdown-menu.*` 无触发框正文；tsc 通过。」
