# 设计还原产线 · 更新日志 · 7月1日

> 合并记录 6月30日-7月1日围绕 Yuwen Agent 数据源接入 demo、foundation、agent rules 做的事。

## 数据源接入 Demo · TikTok Mini Flow

- [决策] 两周目标从“可基本流转 demo”收紧为“视觉严谨、规则可追溯、可与产品 / 研发对齐的 TikTok Ads 数据源接入 case”。导航涉及换肤和产品框架，暂放最后；先补基础组件、foundation 和流程规则。
- [实现] 已建立 `src/features/tiktok-ads-connections/`，包含 DataSourceSpec、数据源管理页、TikTok 空态页、连接列表页、创建连接抽屉、本地 mock 提交流转。
- [验收] ✅已验「Vite 页面可打开 `/pages/data-source-demo`；TikTok 卡片可进入空态；创建抽屉可填写提交；提交后生成 mock 连接；构建通过但页面视觉仍需重做。」

## Foundation 与 Agent Rule · 文档骨架

- [决策] 设计文档和组件 token 都需要纳入，但分层不同：PDF / Figma 是证据源，foundation 是提炼层，token / 组件是执行层，agent rules 是交互判断层。
- [实现] 新增 `docs/references/source-index.md`；新增 `docs/foundations/` 下颜色、字体、间距、圆角、阴影、卡片初稿；新增 `docs/agent-rules/` 下价值体验原则、一致性流程规则、页面校验清单初稿。
- [token] Foundation 初稿引用 `tokens.resolved.json`、`theme.ts`、`color-utils.ts` 中现有 color / spacing / radius / shadow 入口；暂未改 token 生成逻辑。
- [验收] 🔶待验「后续需逐页校准 PDF / Figma 规则，并对照组件实现补 token 映射。」

## 方法论资料 · Figma 原则文档读取

- [决策] `Sens.Design 价值体验设计原则` 进入 agent rules 的页面评审层；`Sens.Design 一致性流程规范` 进入对象流程判断层，优先服务 TikTok 创建 / 添加 / 删除 / 放弃 / 挽留。
- [实现] 已抽取两个 Figma 文件的文字结构，提炼为准确性、高效率、无障碍、生机，以及创建、添加、查看、更多、编辑、删除、放弃、挽留、移除、二次提示规则。
- [验收] ✅已验「Figma 只读抽取完成；未写入 Figma 文件。」

## Shadow Foundation · Helper 与预览页

- [决策] 投影基础层先补通用 helper 和基础样张，不直接改 Card / Drawer / Table 的真实组件样式；组件侧后续按场景逐个接入。
- [实现] `color-utils.ts` 新增 D2、四方向 `buildShadow(level, direction)` 与 `buildActiveRingShadow(handle)`；投影基础样式页改为消费正式 helper，并展示 D1-D4、四方向、active ring 与当前 helper 状态。
- [token] 投影色继续通过 `mask-01-transparent`、`component-active-shadow`、`warning-color-active-shadow` 与 `tokenRgba()` 派生；未手改 `tokens.resolved.json` / `theme.ts`。
- [验收] ✅已验「ReadLints 无报错；`npm run build` 通过；Playwright 打开 `http://127.0.0.1:5174/basic-styles/shadow`，HTML 渲染出 D1-D4、四方向、active ring 与 helper 状态，且 console 无 error。」

## Typography Foundation · 预览页

- [决策] 字体基础层先做验收样张和 token 缺口展示，不直接改真实组件字号；Typography token / helper 后续单独收敛。
- [实现] 新增 `/basic-styles/typography`，展示超大标题、一级到四级标题、正文、辅助信息加粗、辅助文案的字号 / 行高 / 字重，并补 TikTok case 使用对照、当前 token 状态和 token 组合关系。
- [token] 当前 `font-size/s`、`line-height/s` 已有；补充 `font-size/m/l/xl/xxl/display`、`line-height/m/l/xl/xxl/display`、`font-weight/regular/medium/semibold` 的目标口径，不手改 `tokens.resolved.json` / `theme.ts`。
- [验收] ✅已验「ReadLints 无报错；`npm run build` 通过；HTML 字体页可看到完整 Typography token 目标表、字体层级样张和 token 组合关系。」

## Size Foundation · 文档与预览页

- [决策] 固定尺寸、图标尺寸、组件高度和组件专属尺寸候选从 Spacing 中拆出；高度 / 宽度 / icon size 不借用 spacing，即使数值相同也要归到 Size 或组件专属 token。
- [实现] 新增 `docs/foundations/size.md` 与 `/basic-styles/size`，展示基础 size scale、icon size、component height、Tag 尺寸候选和 Card 相关 size 使用；左侧基础样式导航新增「尺寸」。
- [token] 复用 `size/*`、`size/icon/*`、`size/component-height/*`；Tag 的 `tag/padding-inline/s`、`tag/padding-block/s` 先记录为组件专属候选；小输入框 28px 记录为待确认组件高度 token，不使用 `spacing/7x`。
- [验收] ✅已验「ReadLints 无报错；`npm run build` 通过；HTML 可通过 `/basic-styles/size` 查看 Size Foundation 样张和规则。」

## Card Foundation · 基础容器样张

- [决策] Card 先作为基础样式沉淀，不直接等同于可交互业务卡片；TikTok 数据源卡片后续应单独拆为 `DataSourceCard / EntryCard`。
- [实现] 重构 `/basic-styles/card`，改为左侧展示 Card 样张、右侧展示「设计规则 / 数值与 Token 映射」；补 `docs/foundations/card.md`，明确自由容器卡片、标题区组合示例、紫色占位和真实业务组件的边界。
- [token] 样张消费 `spacing/4x`、`radius/l`、`size/icon/m`、`size/component-height/xs`、`tokenRgba()`、`getColorByPath()` 和 Typography helper；Typography 组合已明确为 `font-size/m + line-height/m + font-weight/medium|regular`，但尚未完整进入生成链路。
- [验收] ✅已验「`npm run build` 通过；浏览器打开 `/basic-styles/card`，Card 样张、设计规则和 Token 映射可见，console 无 error。」
- [待做] ⏳待做「Card hover / active / focus / selected / disabled 交互状态；DataSourceCard / EntryCard 业务交互卡片。」

## Divider Foundation · 可消费 Token 入口与样张

- [决策] 分割线作为独立基础样式处理，不只归到 Color；统一 1px 线宽，并区分四个线色的透明版本和非透明版本。
- [实现] 新增 `src/design-system/divider.ts`，提供 `getDividerColor()`、`getDividerBorder()`、`dividerWidthTokens` 和四个 divider token spec；新增 `docs/foundations/divider.md` 与 `/basic-styles/divider` 页面样张；左侧基础样式导航新增「分割线」。
- [token] 建立 `divider/color/deep|outline|light|weak/transparent|solid` 的代码侧 token 入口；透明版本通过 `tokenRgba()` 派生，非透明版本通过 `getColorToken()` 读取；未手改 `tokens.resolved.json` / `theme.ts`。
- [验收] ✅已验「`npm run build` 通过；浏览器打开 `/basic-styles/divider`，四个线色、横向 / 竖向分割线、卡片分割样张、Token 映射和场景准入可见，console 无 error。」
- [待做] ⏳待做「明天与 Typography 一起校验生成链路，决定是否进入 `build-tokens.mjs` 输出结构。」

## Spacing Foundation · 预览页

- [决策] 间距基础层先做验收样张和边界说明，不直接改真实组件 padding / margin / gap；Form / Drawer / Table 的专属 spacing 后续按组件阶段校准。
- [实现] 新增 `/basic-styles/spacing`，展示基础 scale、水平 / 垂直 spacing、spacing 与 size 边界、常用场景、TikTok case 对照和当前问题。
- [token] 样张读取 `spacing/*`、`spacing/horizontal/*`、`spacing/vertical/*` 与 `size/component-height/xl`；未手改 `tokens.resolved.json` / `theme.ts`，公式型值不升全局 spacing token。
- [验收] ✅已验「ReadLints 无报错；`npm run build` 通过；Playwright 打开 `http://127.0.0.1:5178/basic-styles/spacing`，HTML 渲染出基础 scale、水平 / 垂直 spacing、边界表、常用场景、TikTok case 对照与当前问题。」

## Navigation Color Foundation · 文档拆分

- [决策] 导航颜色系统独立于普通 Color Foundation，后续顶导航、侧导航、标题栏、页面主题背景和换肤映射单独维护。
- [实现] 从 `color.md` 拆出 `navigation-color.md`，`color.md` 仅保留导航边界说明和引用；`README.md` 补文档清单。
- [token] 暂不新增 token，不手改 `tokens.resolved.json` / `theme.ts`；保留 `component-active` 与 Figma `#008C64` 的差异待确认。
- [验收] ✅已验「读回 `color.md` / `navigation-color.md` / `README.md`，确认普通 Color 仅保留导航边界说明，Navigation Color 独立承接导航主题色与换肤映射。」

## Color Foundation · 预览页

- [决策] 普通 Color 预览页只承接组件 / 页面通用颜色；功能色标注为可换肤语义色，导航主题色不放入本页。
- [实现] 新增 `/basic-styles/color`，展示四层关系、功能色、状态色、中性色、透明色 / alpha、TikTok case 用色准入和当前问题。
- [token] 样张通过 `getColorToken()` / `tokenRgba()` 读取语义 handle；未手改 `tokens.resolved.json` / `theme.ts`，未新增真实组件颜色。
- [验收] ✅已验「ReadLints 无报错；`npm run build` 通过；Playwright 打开 `http://127.0.0.1:5179/basic-styles/color`，HTML 渲染出四层关系、功能色、状态色、中性色、透明色、TikTok case 用色准入与导航拆分提示。」

## Navigation Color Foundation · 预览页入口

- [决策] 导航颜色是两周工作的关键环节，先在基础样式中建立入口；本轮不做完整导航换肤矩阵，也不改产品壳 / token / theme。
- [实现] 新增 `/basic-styles/navigation-color`，展示与普通 Color 的边界、导航颜色分组、已有代码方向、关键映射状态和当前待补问题。
- [token] 已 token 化的示例通过 `getColorToken()` 读取；未 token 化的顶导航 / 侧导航渐变只记录待映射状态，不在预览页代码里继续硬编码。
- [验收] ✅已验「ReadLints 无报错；`npm run build` 通过；Playwright 打开 `http://127.0.0.1:5180/basic-styles/navigation-color`，HTML 渲染出导航颜色入口、边界、分组、已有代码方向、映射状态和待补问题。」

## 待办 · 未完成欠账

- 明日高优：Typography token 和 Divider token 统一检查，确认哪些只是 helper fallback，哪些必须进入 token 源文件和 `build-tokens.mjs` 生成链路。
- 明日高优：做一次已落地基础样式的 token 绑定检查，覆盖颜色、字体、圆角、间距、尺寸、投影、分割线、卡片。
- 明日高优：Card 补交互状态样张前，先确认 hover、active、focus、selected、disabled 分别消费什么颜色、投影和描边。
- Foundation 数值需继续校准：字体行高、Form / Drawer / Table 的间距规则、Card / Drawer / Table 的投影使用场景、语义 spacing。
- 缺少 Figma 导出的原始 token JSON，当前只能基于 `tokens.resolved.json` / `theme.ts` 做反向整理。
- 缺少数据源管理页四个关键页面的精确 Figma node 和真实 logo / icon 资产。
- 缺少 Drawer / Form / Empty / Tag 的官方组件规范来源，后续实现前需要补设计来源或明确推导依据。
- Button / Search / Select / TableShell 的 token 欠账需按 TikTok demo 使用范围逐步修。
