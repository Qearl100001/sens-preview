# 设计还原产线 · 更新日志 · 8月6日

> 记录对卡片样板间 `/templates/card/entry-settings` 的收口与浏览器验收；以及异常状态 Foundation / `SensEmptyState` 落地。

## 卡片样板间 · 入口配置（entry-settings）

- [决策] 入口配置页无多选/导航选中语义，去掉入口卡片持久 `selected`；点击与双击统一走宿主反馈，不新增独立视觉态。大号入口卡片本轮跳过，留 P1。
- [决策] 栅格换列对齐 Layout Foundation：产品最小画布 `1280px` 保持结构；`1280→3` 列、`1440+→4` 列；删除原先 `<1280` 折叠产品壳的媒体查询。
- [决策] Figma `页面标题栏`（node `1475:52457`）必须用 `SensPageTitleBar variant="landing"` 白底；`SensSectionTitle` 灰底只用于「测试项目 / 通用设置」等分组标题。此前误把页面标题写成灰底分组标题，已回修。
- [实现] `CardEntryTemplatesPage.tsx`：真实剪贴板写入 `sens-preview@0.1.0` + `SensMessage` 成功/失败反馈；页面标题改 `SensPageTitleBar`；分组仍用 `SensSectionTitle`；统计值行高改 `line-height/m`；图标改 `sizeToken="size/icon/m"`。
- [实现] `card-entry-templates.css`：`min-width: 1280px`；栅格默认 3 列、`min-width: 1440px` 升 4 列；移除硬编码 `font-weight: 600` 与移动端壳折叠断点。
- [实现] `templates/card/README.md` 组件映射表纠正：页面标题栏 ≠ 分组标题。
- [token] `font-weight/semibold`、`line-height/m`、`font-size/m`、`radius/xl`、`spacing/horizontal/4x`、`spacing/vertical/4x`、`spacing/vertical/7x`、`size/xxl`、`size/icon/m`、`shadow/D2`（内容向内）、侧导悬浮 `shadow/D4`（组件内）
- [验收] ✅已验「Playwright：1280/1440/1920 列数与壳高；剪贴板读回 `sens-preview@0.1.0`；Message 可见；Enter/Space/点击反馈；无持久 selected；overlay 内容影无、侧导右影；`npm run build` 通过」
- [验收] ✅已验「页面标题栏 landing 白底：heading 基本设置 · 72px · `rgb(255,255,255)`（Playwright computed）」
- [实现] `SensEntryCard` 辅助文案始终包 `SensTips`；CSS 锁 tip 触发层 `display:block; width:100%`，避免省略被撑开；样板间「触达通道」改为超长说明样本。
- [验收] ✅已验「超长辅助文案仍 ellipsis（610>147）；悬停出 SensTips 全文（Playwright hover + tooltip）」
- [验收] ✅已验「2026-08-06 复验全量：1280/1440/1920 列数 3/4/4；标题栏白底；分组灰底；复制剪贴板；点击/Enter/Space/双击；无持久 selected；Tips 浮层未裁切；侧导收起/悬浮投影；目录高亮」

## 异常状态 · Foundation + SensEmptyState

- [决策] 侧栏中文名「异常状态」，路由 `/basic-styles/empty-state`；空态插画不进 Icon registry，落 `src/assets/empty-state/{page,non-page}/`。
- [决策] 页面级大/小插画 266/192；非页面级基础/特殊 100/50。页面级插画→文案 32px 尚无 `spacing/8x`，用组件常量 `EMPTY_STATE_PAGE_STACK_GAP`（To Confirm）。
- [实现] 从 Figma 导出页面级 5 + 非页面级 5 PNG；扩展 `EmptyStateIllustrations.ts`；新增 `SensEmptyState` + `empty-state.css`；`SelectDropdownEmpty` 改为薄封装消费 `scope="non-page" size="special"`。
- [实现] `docs/foundations/empty-state.md`；样张页 `EmptyStateBasicStylePage`；`BASIC_STYLE_NAV` / `App.tsx` 路由；Foundations 索引补行。
- [token] 页面级：`font-size/l` · `line-height/l` · `font-weight/semibold` · `spacing/vertical/4x` · `text-color-transparent` @0.9 · `text-sub-color-transparent` @0.58 · `link-color`；非页面级：`spacing/5x` · `spacing/vertical/3x` · `spacing/vertical/1x` · `font-size/m|s` · `line-height/m|s` · `font-weight/regular`
- [验收] ✅已验「Playwright MCP：20 个实例；页面大 266/gap32/16·24·600；页面小 192；非页面基础 100/pad20/gap12；特殊 50；侧栏「异常状态」选中；`tsc --noEmit` 通过」
- [实现] `TableShell` / `DataSourceEmptyState` 空态改为消费 `SensEmptyState scope="non-page" size="base"`；表格壳保留 padding / min-height，去掉行高推导插画边长。
- [验收] ✅已验「Playwright：`/components/table` 空态已为 `.sens-empty-state` · `size=base` · img 100 · gap 12 · pad 0」
- [验收] ✅已验「样张全量无破图；Table 三态；下拉 special 50×2；DataSource 下钻空态（临时关 recording mock 后已恢复）」
- [决策] 用户确认：Empty State Foundation 升 Verified；成熟度仍 Pilot。
- [实现] `SelectDropdownShowcasePage` 增 `optionsLoadFailed` + `defaultOpen` 真实 Demo（加载失败不进六面矩阵，单独打开态验收）。
- [验收] ✅已验「下拉打开态 loadFailed：Portal 内 `.sens-empty-state` special/img50 · 标题加载失败 · 含刷新」
- [验收] ✅已验「Empty State Foundation → Verified（样张/Table/DataSource/下拉打开态）」


## Spacing · 补 spacing/8x

- [决策] 用户确认补 foundation `spacing/8x = 32`（及 `spacing/vertical/8x` 别名），承接页面级空态插画→文案间距。
- [实现] `tokens/source/figma/unit.json` 增档；`tokens:build`；`SensEmptyState` 改读 `spacing/vertical/8x`；`EMPTY_STATE_PAGE_STACK_GAP` 改为同值别名；`spacing.md` / 样张页 / empty-state 文档同步。
- [token] `spacing/8x` · `spacing/vertical/8x`
- [验收] ✅已验「`tokens:check` 通过；resolved `spacing/8x` / `vertical/8x` = 32」

## Typography · Sens 字体栈单一来源灌入

- [决策] 字体主权在 Sens.Design；antd 仅承接 `token.fontFamily`，不使用 seed 默认（Roboto 等）。栈只维护在 `SENS_FONT_FAMILY`，不新增字体族 token。
- [实现] `buildAntdTheme`（`theme.ts` + `build-tokens.mjs` 模板）统一灌入 `SENS_FONT_FAMILY`；`applySensFontFamilyToDocument` 挂到 `html, body, #root`；`main.tsx` / `agent-eval-main.tsx` 入口调用；`appearance-antd` 去掉重复 fontFamily。
- [实现] `docs/design-system.md` §4.1 与 `typography.md` 对齐正式栈（含 Helvetica + Emoji）及灌入路径。
- [验收] ✅已验「Playwright：`html/body/#root`/`.ant-layout`/`.ant-btn` 均为 Sens 栈；含 PingFang / YaHei / Emoji；无 Roboto；`#sens-font-family-root` 已注入」

## Typography · ui 字号/字重硬编码债清零（第 1+2 批）

- [决策] 真实组件 + 预览矩阵 CSS 一律经 `getTypographyToken` → CSS 变量消费；视觉数值不变（12/14/400/500/600）。不扫 preview 页零星 TSX、不碰 agent-eval monospace。
- [实现] 组件本体：`select-dropdown` / `dropdown-menu` / `badge` / `form` / `search` / `inputnumber` / `checkbox`·`radio` / `entry-card` / `empty-state` / `select-trigger`；预览：`button` 矩阵、`fab-group`、`*-preview.css`、`search-preview`。
- [token] `font-size/s|m` · `line-height/s|m` · `font-weight/regular|medium|semibold`
- [验收] ✅已验「`src/ui/**/*.css` grep 无裸 px 硬编码；`tsc --noEmit` 通过；Button 矩阵标题 computed 仍为 12/18/500」


## Typography · SchemaManagementPage 字号字重 token 化

- [决策] 业务/案例页字号字重改为 `getTypographyToken`；preview showcase / agent-eval 仍另轨。
- [实现] `SchemaManagementPage.tsx`：12→`font-size/s`；14/22→`m`；20/30→`xxl`；500→`medium`。
- [验收] ✅已验「页内无裸 fontSize/fontWeight 数字；`tsc --noEmit` 通过」


## Table · 基础表格信息区对齐复合样张

- [决策] 刷新跟「数据更新于」文案（`linkWeak`）；列设置仅 `setting` icon-only；与复合筛选表格共用入口组件。
- [实现] `TableInfoRefreshableSummary` / `TableInfoColumnSettingButton`；`TableShowcasePage` 增「数据更新」态；`CompositeTablePage` 改消费共用入口；`table.md` API 示例同步。
- [验收] ✅已验「Playwright：`/components/table` 默认数据更新态——文案含更新时间；刷新在 info-text 内；列设置在 info-extra 且无「设置」文案；`tsc` 通过」


## Top Navigation · Sensors Data Logo 替换占位

- [决策] Figma `18530:73096` 白标 logo 替换顶导左上角「SENS」文字占位；尺寸固定 58×20。
- [实现] 资产 `public/brand/sensors-data-logo.svg`；组件 `SensTopNavLogo`；`TopNavigationShowcasePage` / `SensTopNavigation` 消费；`top-navigation.md` 补行。
- [验收] ✅已验「Playwright `/components/top-navigation`：`img[alt=Sensors Data]` src=`/brand/sensors-data-logo.svg`；computed 58×20；`tsc` 通过」

## Top Navigation · 对齐 Figma 字号与结构（2626:11857 / 2693:13097）

- [决策] 上栏：项目名 + 箭头（无灰胶囊）；账号名 + 独立角色 pill + 箭头。下栏：去掉「功能入口」文案，仅九宫格 + 分割线。间距对齐稿：水平 `spacing/4x`；Logo↔项目 `6x`；工具 `4x`；下导项 `2x@<1440` / `4x@≥1440`。
- [决策] 字号：下导项 `font-size/l`·`line-height/l`（选中 semibold / 默认 regular）；项目名 `m`；账号名 `l`·semibold；角色 `s`。角色 pill 圆角 12 / 竖直 pad 3 尚无 token，作结构常量。
- [实现] `TopNavigationShowcasePage.tsx` / `SensTopNavigation`；`top-navigation.md` 结构与 Token 表同步。
- [token] `font-size/l|m|s` · `line-height/l|m|s` · `font-weight/regular|semibold` · `spacing/2x|4x|6x` · `theme-top-role-background`
- [验收] ✅已验「Playwright：下导项 16/24（默认 400 / 选中 600）；无「功能入口」；项目切换文案为当前项目名且透明底；账号 16/24·600 + 角色 12/18 pill(radius12/pad3×8) + 箭头；pad 16；1440 gap=16 / 1280 gap=8；`tsc` 通过」


## Top Navigation · 带下拉主导航项打开态箭头向上

- [决策] Figma 下拉项矩阵（`2626:14647` 等）：关闭 `nav-down`；浮层打开时箭头旋转 180° 呈向上（与项目/账号一致）。选中短线与打开态独立——选中关闭仍为向下。
- [实现] `TopNavigationShowcasePage`：营销活动 `isDropdownOpen` → `rotate(180deg)`；文案↔箭头 gap 改 `spacing/1x`；`top-navigation.md` 补规则。
- [验收] ✅已验「Playwright：打开营销菜单 transform=rotate(180)；aria-expanded true；关闭恢复；菜单可见」


## Top Navigation · 右上角色选择器对齐 Figma（14608 / 14606）

- [决策] 结构：账号名 +（角色 pill + 箭头）成组；名↔组 `spacing/2x`，pill↔箭头 `spacing/1x`。默认 `theme-top-text` + down；悬停/打开 `theme-top-text-hover`；打开箭头旋转向上。
- [实现] `TopNavigationShowcasePage` 账号区；`top-navigation.md` 同步。
- [验收] ✅已验「Playwright：结构 gap 8/4；名 16/24·600；pill 12/18 pad3×8 r12；悬停色 white；打开 rotate180 + 菜单」


## Top Navigation · 主导航项选中不跳动（顶距恒 8px）

- [决策] 对齐 Figma `2693:13109` / `13110` 与用户约束：文字顶距恒 `8px`；短线始终占位（未选透明），避免选中切换时文字上下跳。
- [实现] 导航项 padding 固定 `8px 16px 1px`；短线 3px 占位；箭头区 padding 不再随选中变化。
- [验收] ✅已验「Playwright：选中/未选 textOffset 皆 8；padTop 恒 8px；textHeight 24；短线仅选中可见」


## Top Navigation · 下拉箭头与文字垂直居中

- [实现] 去掉箭头 `paddingTop: 4`；箭头容器高度对齐 `line-height/l`（24）并 `align-items: center`，与文字行光学居中。
- [验收] ✅已验「Playwright：营销活动文字/箭头中心 delta=0；打开态仍 rotate180」


## Top Navigation · 「更多」右置 + 带下拉项统一打开态

- [决策] 「更多」固定右端（下栏右 padding `spacing/4x`=16）；前加分割线。营销活动 / 内容管理 / 项目设置 / 更多共用同一套箭头居中与打开旋转。
- [实现] 主导航拆 primary / more；`openNavDropdown` 通用浮层；演示菜单表 `NAV_DROPDOWN_MENUS`。
- [验收] ✅已验「Playwright：更多右距 16；四项打开均 rotate180 + 菜单」


## Top Navigation · 项目切换浮层（搜索 + 6 可见 + 底遮罩）

- [决策] Figma `14530:36817`：浮层用实时搜索 `SearchInput`；选项最多可见 6 条（36×6 + gap×5）；超出滚动；末项上白色渐变遮罩。
- [实现] `TopNavigationShowcasePage` 项目面板；演示数据 >6 条；过滤无匹配空态；选中 `font-weight/medium`。
- [验收] ✅已验「Playwright：SearchInput 占位「搜索项目名称」；列表 maxH 256；项高 36；>6 时 mask 36；可滚动」


## Top Navigation · 账号角色菜单 + 语言子菜单选中态

- [决策] Figma `14523:37169` 主菜单：图标行 / 分割线 / 右箭头子菜单 / 敏感信息开关（26×16）。`14523:37168`+`14519:36771`：可选中子项默认对勾 + medium；未选 regular；悬停绿底绿字。
- [实现] `TopNavigationShowcasePage` 账号浮层重构；系统语言子菜单；`check` / `personal` 等 SensIcon；`switch-background` / `component-active`。
- [验收] ✅已验「Playwright：7 项 + 2 分割线；开关 26×16 可切换；语言子菜单选中对勾 medium；选 English 后重开仍勾选」


## Top Navigation · 项目搜索空态 + 首屏遮罩滚动消失

- [决策] 搜索无结果消费 `SensEmptyState`（non-page / special / noResult，标题「无匹配项目」）。白渐变遮罩仅首屏（scrollTop=0）盖在末可见项上，滚动后消失，回顶再出现。
- [实现] `TopNavigationShowcasePage` 项目面板；`onScroll` 驱动 `projectListScrolled`。
- [验收] ✅已验「Playwright：顶遮罩有 / 滚动后无 / 回顶有；空态 sens-empty-state type=noResult size=special 标题无匹配项目」


## Top Navigation · 语言子菜单与触发行顶对齐（25828:22018）

- [决策] Figma `25828:22018`：系统语言子菜单顶部与「系统语言」行顶对齐（非整块主菜单顶）；水平 `right:100%` 贴齐主菜单，无 gap。
- [实现] `TopNavigationShowcasePage`：子菜单改为相对主菜单绝对定位；`useLayoutEffect` 实测触发行 offset（约 51px）。
- [验收] ✅已验「Playwright：submenu.top − 系统语言.row.top = 0；menu.left − submenu.right = 0；style.top=51px」

## 待办 · 未完成欠账

- 大号入口卡片分组仍未录入本页（文档写大号+小号，实现仅小号）
- 剪贴板不可用路径按产品确认本轮不验
- 第二卡片业务样板间页面仍待补；本页验证状态仍保持 Pilot / Pending（未升 Verified）
- 空态默认中文文案是否全部进 i18n（下拉已 i18n）
- preview 页零星字号硬编码（showcase TSX）可另轨
- 顶导角色 pill `radius/12`、竖直 pad `3` 是否补 foundation 档待确认
