# 设计还原产线 · 更新日志 · 7月30日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## Title · 右侧操作区 / 帮助选填 / Demo 排版收口

- [决策] 取消「标题到右侧操作 = 右侧 padding 推导」To Confirm；主内容与按钮组最小分隔取 Figma 64（`spacing/10x + spacing/horizontal/6x` 凑出）。仅通用大标题展示 `actions`；小标题与 productLine 不渲染按钮组。补 `help` / `optional`；顺序标题→帮助→选填→辅助。
- [实现] `SensSectionTitle` API；`section-title.css` 变量改名 `actions-min-gap`；`TitleShowcasePage` 五组 Demo（基线 / 通用大含帮助选填按钮组 / 通用小 / 专用大 / 专用小）；`title.md` / `title.design.md` 同步（Figma `3819:8702`）。
- [token] `spacing/horizontal/4x`（条左右 pad、按钮组 gap）、`spacing/horizontal/1x`（标题↔meta）、`size/component-height/xxl`、`background-transparent-grey` @4%、`text-color-transparent` @0.9、`text-sub-color-transparent` @0.58、`icon-color-transparent`、`SensIcon name="help"`；未改生成物。
- [验收] ✅已验「Playwright `/components/title`：通用大高 48、左右 pad 16、按钮 gap 16；帮助 hover Tips；小标题与 productLine 无按钮组；Demo 组间 16 / 组内 8」

## Title · 选填 / 辅助字号字重对齐 Figma

- [决策] 选填跟标题同档字号、细体 400（大 16/24，小 14/22）；辅助文案保持 `font-size/s`，字重改为 `regular`；色仍 `text-sub-color-transparent`（Figma 子夜黑/09 → 语义 handle）。
- [实现] `section-title.css` 拆开 optional / description；`SensSectionTitle` 增 `--sens-section-title-meta-weight`；`title.md` / Demo matrix 同步。
- [token] `font-size/l|m|s`、`line-height/l|m|s`、`font-weight/regular`、`text-sub-color-transparent`；未改生成物。
- [验收] ✅已验「Playwright：大选填 16/24/400；小选填 14/22/400；辅助 12/18/400；色 text-sub @58%」

## Title · 帮助 icon 光标

- [决策] 帮助仅悬停出 Tips、不可点 → 用图1 `cursor: default`（非 `help`、非图2 `pointer`）；`pointer` 只给可点击对象。光标**使用场景**主定义进 `docs/foundations/cursor.md`，不在 `title.md` 另立语义。
- [实现] `section-title.css` → `var(--sens-cursor-default)`；`cursor.md` §4.1；`title.md` 只引用。
- [验收] ✅已验「文档 + 代码」

## Title · 验收轻收口（文档）

- [决策] Demo 与矩阵（规则摘要）同为验收对象；通用小左右 pad 写入 Title token 表（`spacing/horizontal/2x`，实现已有）；64 公式归入 spacing foundation **本轮不做**。
- [实现] `title.md` / `title.design.md` / `TitleShowcasePage` 矩阵说明；未改 spacing.md、未新增 token。
- [验收] ✅已验「文档对照」

## Pagination · P0 边界翻页 Tooltip（>10 页路径）

- [决策] >10 页 antd 折叠路径的 `prevIcon` / `nextIcon` 必须与 ≤10 / 简版一致传入边界 `disabled`，禁用 hover 文案为「当前已在首页 / 尾页」。
- [实现] `SensPagination.tsx` 仅改 `prevIcon` / `nextIcon` 的 `disabled` 传参；未扩 P1、未动 CSS / token。
- [验收] ✅已验「Playwright `/components/pagination`：>10 页到首页 prev=`当前已在首页`；到末页 next=`当前已在尾页`」
- [实现] 矩阵补「边界翻页 Tips」四样张（≤10 首页/末页、>10 首页/末页），对照两条路径文案一致；未做简版/禁用 P1。

## Pagination · P1 轻收口

- [决策] 去掉键盘 `tabIndex` 与 `.ant-*` 清单两项；其余 P1 落地。`showQuickJumper` 未传时：总页 ≤10 默认关、>10 默认开（显式 props 可覆盖）。矩阵补简版 / 整组禁用；工程落点改为 `PaginationShowcasePage`。文案 token / 派生宽度仍后置。
- [实现] `SensPagination.tsx` 默认跳页策略；`PaginationShowcasePage` 矩阵；`pagination.md` / `pagination.design.md`。
- [验收] ✅已验「矩阵：≤10 无跳页、>10 有跳页；含简版与整组禁用；Demo 25 条无跳页 / 1000 条有跳页」

## Drawer · P0 Esc 关闭 + 焦点进入/回收

- [决策] `role="dialog"` 最小契约：Esc → `onClose`；打开聚焦面板内首个可聚焦控件（否则面板本身）；关闭后还焦。完整 focus trap 本轮不做。
- [实现] `SensDrawer.tsx`；`drawer.md` / `drawer.design.md` 同步。
- [验收] ✅已验「Playwright `/components/drawer`：打开焦点进返回按钮；Esc 关闭；焦点回到「打开中抽屉」」

## Drawer · 三档宽度 + 有/无蒙层

- [决策] 补 `large`（80%）；宽为比例动态 `clamp(@1440, Nvw, @1920)`；`SENS_DRAWER_WIDTH` 保留 1440 对照。蒙层直读 `mask-01-transparent`（禁止再 0.45）。无蒙层用透明点击层点外关闭。
- [实现] `SensDrawer.tsx`；`DrawerShowcasePage` 三尺寸 + 蒙层开关；`drawer.md` / `drawer.design.md`。
- [验收] ✅已验「Playwright：三档宽≈30/60/80vw（clamp）；无蒙层点外关闭；有蒙层 rgba(11,12,13,0.6)=mask-01」

## Drawer · 有/无蒙层关闭路径

- [决策] 有蒙层：点蒙层不关，须返回 / 操作按钮（Esc 仍关）；无蒙层：点面板外可关。
- [实现] `SensDrawer.tsx` 蒙层改为非交互 `div`；无蒙层保留透明点击层；文档与 Demo 文案同步。
- [验收] ✅已验「Playwright：有蒙层点遮罩不关；点取消关；无蒙层点外关」

## Drawer · P1 滚动锁 / 层级 / 宽度契约 / Foundation / a11y

- [决策] 打开锁定背景滚动；`SENS_DRAWER_Z_INDEX=1000` + `maxWidth=calc(100vw−2×spacing/horizontal/6x)` 写入文档；宽度比例/对照/视口界为组件契约常量 Ready（`vw`/`clamp` 不进 semantic-unit）；Foundation `spacing.md` 去掉「抽屉 padding 依赖 antd」过时句；`aria-labelledby` 挂 `SensTitleBar` 标题。
- [实现] `SensDrawer.tsx`、`SensTitleBar.tsx`（`titleId`）、`ui/index.ts`、`DrawerShowcasePage`、`drawer.md` / `drawer.design.md`、`docs/foundations/spacing.md`。
- [验收] ✅已验「Playwright：body overflow 打开 hidden / 关闭还原；z-index=1000；aria-labelledby 对齐标题「中 · 60%抽屉」；maxWidth 解析为 calc(100vw−48)」

## Drawer · 开合动效

- [决策] 右侧抽屉自右缘滑入（`translateX(100%)→0`）；有蒙层同步淡入淡出。时长 240ms，曲线缓入缓出 `cubic-bezier(0.42, 0, 0.58, 1)`；`prefers-reduced-motion: reduce` 时时长 0。关闭先退场再卸载。不升 motion foundation。
- [实现] `SensDrawer.tsx`（`rendered`/`entered`）；导出 `SENS_DRAWER_MOTION_DURATION_MS` / `EASING`；`drawer.md` / `drawer.design.md` / Demo 矩阵。
- [验收] ✅已验「Playwright：进场中 translateX>0 → 到位 matrix 0；transition 0.24s cubic-bezier(0.42,0,0.58,1)；蒙层 opacity 同步；关闭后退场后 dialog 卸除」

## Drawer · 退场 size 跳变修复

- [决策] Demo 关闭时勿把 `size` 回落 `medium`；组件退场冻结 `displaySize`，防止「小抽屉先变宽再滑出」。
- [实现] `DrawerShowcasePage` 拆 `open`/`size`；`SensDrawer` 退场锁档；`drawer.md` 补充约定。
- [验收] ✅已验「Playwright：小抽屉关闭过程 layout 宽始终 ≈30vw（559），无跳到 medium；退场后卸除」

## Drawer · 动效后焦点进入回归修复

- [决策] `rendered` 延迟挂载后，焦点 effect 须等面板入 DOM（`open && rendered`）再聚焦；`previousFocus` 仍在 `open` 变 true 时立即记录。
- [实现] `SensDrawer.tsx` 拆开还焦记录 / 进焦+Esc 两个 effect。
- [验收] ✅已验「打开中抽屉焦点进「返回」；Esc 后还焦「打开中抽屉」」

## Drawer · 整轮复验（7/30）

- [决策] 本轮抽屉可过；边界仍后置：完整 focus trap、多层抽屉、底部固定操作区、`prefers-reduced-motion` 手验。
- [验收] ✅已验「Playwright 26/26：三档宽@1440；@1600 medium≈960；圆角/标题72/pad 16·24·24；z=1000；maxWidth；mask-01；Esc/焦点；有蒙层点遮罩不关；无蒙层点外关；滚动锁；动效曲线；小抽屉退场宽稳定；aria-labelledby」

## Cursor · P0 轻收口

- [决策] 成熟度与 README 对齐「系统关键字、PNG 仅示意」；表单帮助 `cursor: help` 改为 `--sens-cursor-default`（对齐 §4.1）；鼠标页默认进「数值样张」作验收入口。
- [实现] `docs/foundations/cursor.md` / `README.md`；`form.css`；`form.md`；`BasicStylePageLayout` 增 `initialTab`；`CursorBasicStylePage`。
- [验收] ✅已验「打开 `/basic-styles/cursor` 默认「数值样张」且 move=move；form 帮助 computed cursor=default」

## Cursor · P1 轻收口

- [决策] 去重 Tabs 对 `cursors.css` 的重复 import；conventions 补光标消费句；`ui` 导出 `sensCursorValue` / `SENS_CURSOR_*`；`copy`/`no-drop` 同 Figma 帧确认为同帧非笔误；`grab`/`grabbing` 真实 Demo 标边界后置（不造假 Demo）。硬写 `cursor` 全仓迁移仍后置。
- [实现] `SensTabs.tsx`；`conventions.md`；`ui/index.ts`；`cursors.ts`；`docs/foundations/cursor.md`。
- [验收] ✅已验「文档 + 导出对照；Tabs 仍依赖全局 `App` 引入的 cursors.css（页签拖拽 move 规则未改）」

## Cursor · ui 去硬写同义替换

- [决策] `src/ui` 内 `cursor: pointer|default|not-allowed` 一律改为 `--sens-cursor-*` / `sensCursorValue()`；语义不变。features / 预览二期。
- [实现] 13 个组件 CSS + `SensMessage` / `feedbackShared` / `SensTag` / `SensBreadcrumb` / `ProductShellSideNavigation` / `SensDrawer`；`cursor.md` / `conventions.md` 写明禁止硬写。
- [验收] ✅已验「`rg`：`src/ui` 除 `cursors.css` 变量定义外无硬写 pointer/default/not-allowed/help」

## Cursor · features / 预览硬写清零

- [决策] 二期清单同义替换：`sensCursorValue`；语义不变。
- [实现] `ConnectionTableActions` / `DataSourceEntryCard` / `EvalProductionSection`；`TopNavigationShowcasePage` / `LayoutBasicStylePage` / `CardBasicStylePage`。
- [验收] ✅已验「`rg`：features + preview + ui 无硬写 pointer/default/not-allowed/help」

## TitleBar · P1 轻收口

- [决策] 抽屉标题高与页面同挂 `size/component-height/title-bar`；矩阵标规则摘要；Demo 消歧；删除无引用 `DrilldownTitleBar`；页面标题 `heading`+可选 `titleId`；design 对齐措辞与「有面包屑操作距顶 20」一致；面包屑**可独立成篇**本轮只记边界不拆代码。
- [实现] `SensTitleBar.tsx`；`SensPageTitleBar.tsx`；`TitleBarShowcasePage`；`title-bar.md` / `title-bar.design.md`；删除 `DrilldownTitleBar.tsx`。
- [验收] ✅已验「抽屉根节点 h=72 padR=24 主题底；下钻 94「设备白名单（下钻）」padL=2；矩阵含规则摘要；页面标题 heading×4」

## TitleBar · 抽屉标题与页面标题栏对齐 heading

- [决策] 抽屉标题必须走 `SensTitleBar`；标题节点与 `SensPageTitleBar` 一致挂 `role="heading" aria-level={1}`（并继续被 Drawer 注入 `titleId` / `aria-labelledby`）。
- [实现] `SensTitleBar.tsx`；`title-bar.md`；`drawer.md` / `drawer.design.md`。
- [验收] ✅已验「Drawer 打开：heading「中 · 60%抽屉」id 与 dialog aria-labelledby 一致」

## Breadcrumb · 独立成篇 + 省略态 more 图标

- [决策] 面包屑从 TitleBar 文档拆出独立成篇；省略态中间项对齐稿用 `SensIcon name="more"` · `size/icon/s`（14），不用文字 `...`；下拉折叠本轮不做；当前项颜色沿用 `text-color-transparent` @0.9（稿部分样张与祖先同灰，不改映射）。
- [实现] `SensBreadcrumb.tsx`；新建 `breadcrumb.md` / `breadcrumb.design.md`；`title-bar.md` / `title-bar.design.md` / `README.md` 改为引用。
- [token] `size/icon/s`、`spacing/1x`、`font-size/s` + `line-height/s` + `font-weight/regular`、`text-sub-color-transparent` @0.58、`text-color-transparent` @0.9、`link-color` / `link-active-color`。
- [验收] 🔶待验「独立 showcase 省略态中间为 more 14px」

## Breadcrumb · 独立 showcase 路由

- [决策] 面包屑作为基础组件单独进侧栏与路由，不再在 TitleBar 页挂独立 Demo；TitleBar 仅保留下钻组合样张。
- [实现] `BreadcrumbShowcasePage`；`App.tsx` `/components/breadcrumb`；`PreviewShell` 侧栏「面包屑」；TitleBar 页去掉独立面包屑块。
- [验收] ✅已验「侧栏 `/components/breadcrumb`；TitleBar 页无独立面包屑 Demo」

## TitleBar · 面包屑拆分后复验

- [决策] 拆分后 TitleBar 只验组合布局与文档引用；面包屑形态归独立页。
- [验收] ✅已验「Playwright `/components/title-bar`：landing 72/94 白底 pad 24；下钻 72/94 主题底 padL=2 padR=24；面包屑距顶 11、到标题 2；操作距顶 20；省略 more 14；heading×6；右侧文档含 breadcrumb.md 引用、无独立面包屑 Demo。抽屉 72 / padR 24 / heading。icon 顶≈34（标 35，±1）。landing 有说明+按钮时标题→辅助实测 5（row 被按钮撑到 32 + rowGap 4），下钻有说明为 4」

## Breadcrumb · 文字色交互修复（对齐 Figma 1505:6166 / 6165）

- [决策] 祖先默认 `text-sub` @0.58；悬停 `link-color`、点击 `link-active-color`（对齐 linkWeak），不得一直保持默认灰；当前项仍 `text-color` @0.9。交互色改 CSS `:hover`/`:active`，预览板用修饰类画静态四态。
- [实现] `breadcrumb.css`；`SensBreadcrumb.tsx` 去掉 React hover state；`BreadcrumbShowcasePage` 补四态样张；`breadcrumb.md` / `breadcrumb.design.md` 同步 Figma 节点。
- [token] `text-sub-color-transparent` @0.58、`text-color-transparent` @0.9、`link-color`、`link-active-color`、`size/icon/s`、`spacing/1x`。
- [验收] ✅已验「Playwright `/components/breadcrumb`：祖先默认 rgba(8,18,38,0.58)；真实 hover→link-color #3170EB；current @0.9；矩阵静态 hover/active/current 同 token」

## Breadcrumb · 当前项同色 + 省略下拉

- [决策] 当前项与祖先默认同为 `text-sub-color-transparent` @0.58，不再用主文本加深；省略 `more` 点击展开 `SensDropdownMenu`，列出 `items.slice(1,-1)`（对齐稿下拉单行）。
- [实现] `SensBreadcrumb.tsx` / `breadcrumb.css`；`BreadcrumbShowcasePage`；`breadcrumb.md` / `breadcrumb.design.md`。
- [token] `text-sub-color-transparent` @0.58、`link-color`、`link-active-color`、`size/icon/s`；浮层复用 Dropdown Menu token。
- [验收] ✅已验「Playwright：当前项与祖先默认同 rgba(8,18,38,0.58)；点击 more 弹出二级/三级/四级」

## Breadcrumb · P1 轻收口

- [决策] 补 `aria-current="page"`、可点项 `:focus-visible`；Demo 补 maxWidth 160 截断；文档写明下拉无 onClick 项仅关浮层。
- [实现] `SensBreadcrumb.tsx`；`breadcrumb.css`；`BreadcrumbShowcasePage`；`breadcrumb.md` / `breadcrumb.design.md`。
- [验收] ✅已验「Playwright：aria-current=page；超长祖先/当前宽=160；focus outline 2px link-color」

## Breadcrumb · Demo 文案收口

- [决策] Demo 标题去掉 Figma 节点号与实现细节括注，只保留「普通态 / 省略态 / 单项过长截断」。
- [实现] `BreadcrumbShowcasePage`。
- [验收] ✅已验「Demo 文案对照」

## Alert · 链接对齐轻提示

- [决策] Alert `link` 槽与 Message 同用 `SensMessageLink`（14/22），禁止 Demo 再用 `SensButton tone="link" size="small"`；有辅助文案时链接/关闭与标题行对齐。
- [实现] `AlertShowcasePage` / `AlertTypesPreview`；`SensAlert` + `FeedbackLinkSlot` / `FeedbackCloseButton` `alignWithTitleRow`；`alert.md`。
- [token] `font-size/m`、`line-height/m`、`link-color` / `link-hover-color` / `link-active-color`、`spacing/horizontal/4x`。
- [验收] ✅已验「Playwright：链接 14/22 高22；有辅助时 titleTop=linkTop；非 ant-btn」

## Alert · 默认宽通栏

- [决策] 警告默认**通栏**（`width: 100%`），与灰底标题同；不写死 400。页内常见于标题下方、表格/筛选等业务内容上方。依据：复合表格 · 提示信息位置 `706:33692`（警告宽 = 内容区宽）。
- [实现] `alert.md` 尺寸/放置/待补；`AlertShowcasePage` / `AlertTypesPreview` 去掉 Demo·矩阵 `maxWidth: 480`。组件本身已是通栏，未改 `SensAlert` 容器样式。
- [验收] ✅已验「Playwright：Demo/矩阵 `role=alert` 宽 = 父容器宽（737）；高 36；文档无 400 开放项」

## Checkbox · P1 收口

- [决策] ① 外环（focus-visible / active）立即可见，`box-shadow` 不参与过渡；② 禁用悬停走真实 `:hover`（与矩阵 preview class 共用 token）；③ Demo 补纵向组 / 只读 / 无文案 aria-label / `itemHeight="content"`；④ `readOnly` 能力标 Ready，业务边界按需。
- [实现] `checkbox.css`；`CheckboxShowcasePage`；`checkbox.md` / `checkbox.design.md`。
- [token] `component-active` / `buildActiveRingShadow("component-active-shadow")`；禁用悬停 `background-transparent-grey` @4%、`divider/color/weak/transparent`、`text-color-transparent-disable-hover` @24%。
- [验收] ✅已验「Playwright：Tab 外环立即可见（2px active-ring）；禁用 hover 底/边变化；Demo 含纵向组 / 只读 aria-readonly / control-only aria-label / content-height」

## Checkbox / Radio · 帮助图标 Ready + 5px Deferred

- [决策] 帮助图标并非 Missing：消费已录入 `SensIcon name="help"`（HelpIcon · 输入辅助 · 16 · dualTone），与 Form / Title 同源；经 `helpIcon` 槽位传入。组上下字面 **5px**：当前不加；与 TextArea 上下 5 等一并观察，**满第 5 处场景再升组件内部 token**，不进 foundation / form spacing。
- [实现] `checkbox.md` / `checkbox.design.md` / `radio.md` / `radio.design.md`；`CheckboxShowcasePage` / `RadioShowcasePage` Demo 挂 help + Tips，点击 `stopPropagation` 防误切换。
- [token] `SensIcon name="help"`、`size/icon/m`、`icon-color-transparent`；未新增源 token。
- [验收] ✅已验「Playwright：Checkbox/Radio Demo 有 help；点击帮助不切换勾选/单选」

## 待办 · 未完成欠账

- 轻提示：加载资产仍 Missing。
- Checkbox / Radio：组上下字面 5px 与 TextArea 等一并观察，满第 5 处再升组件内部 token（Deferred）。
- Title：表单样板间组合仍按样板间阶段补。
- Pagination P1：文案 token / 跳页与简版输入宽度派生尺寸（等多组件文案策略与 Figma 命名后再提升）。
- Drawer：完整 focus trap、多层抽屉 / 底部固定操作区仍后置。
- Cursor：`grab`/`grabbing` 待画布/地图真实 Demo。
- Breadcrumb：单项 `maxWidth: 160` 是否进 size token（仍后置）。
- TitleBar（低优）：landing 有说明 + 右侧按钮时，标题行被按钮高度撑高导致标题→辅助视觉间距≈5（规则 4）；是否与下钻固定布局对齐。
