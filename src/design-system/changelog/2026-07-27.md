# 设计还原产线 · 更新日志 · 7月27日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## SensD 鼠标态 · 走查页示意 PNG 清晰度（64@2x）

- [决策] 走查卡左侧示意改为 **64×64 画布 / CSS 显示 32px（2x）**；真实指针仍为系统 `cursor` 关键字，示意不参与 `url()`。
- [实现] Figma `IBBF40Lst6uPPJf70pi0bh` 全量 24 态 `download_assets` png@2 → 居中贴入 `public/cursors/<name>.png`（超高节点等比缩入画布）。
- [实现] `CursorBasicStylePage`：去掉示意 `opacity: 0.9`；`imageRendering` 用 `-webkit-optimize-contrast`；次要说明改 `text-color` / `text-sub-color`。
- [实现] `docs/foundations/cursor.md` 注明示意 PNG 为 64@2x 展示用。
- [验收] ✅已验「`file public/cursors/*.png` 均为 64×64」
- [验收] 🔶待验「浏览器打开 `/basic-styles/cursor` 目视示意清晰度；系统光标 hover 仍正确」

## SensD 鼠标态 · 去掉示意热区浅底

- [决策] 走查卡示意热区不再铺 `colorFillAlter`，避免图标后出现浅色白块。
- [实现] `CursorBasicStylePage` 热区容器去掉 `background` / `borderRadius`。
- [验收] 🔶待验「浏览器 `/basic-styles/cursor` 示意区无独立浅底」

## 待办 · 未完成欠账（仅写在最新一天）

- SensD 鼠标态：浏览器验收 `/basic-styles/cursor` 系统光标切换、示意清晰度与无热区浅底；页签拖拽系统 `move`。
- 走查回修：浏览器复核 `/components/tabs` 小胶囊居中与态色；`/components/select-dropdown` 禁用菜单项色。
- Token 直读第三期：浏览器抽样 `/components/badge`、PreviewShell / basic-styles 色距对照。
- 筛选表格：列设置面板、复杂排序、筛选结果 ↔ `foundTotal` / 分页真实闭环。
- 录入型表格：校验、真实增删与滚动规则；树 / 嵌套交叉表格仍待收录。
- 表格 loading GIF。
- Button、Form、Table、Top Navigation 等基础组件的 Pending 浏览器验收与状态矩阵仍待逐项完成。
- Navigation Color 的其他 Product Shell Theme 矩阵、全局 Functional Skin、Card 基础组件与 Foundation 语义覆盖验收仍待后续推进。
- Preview `getPreviewTokens` 属性名仍沿用 antd ThemeToken 命名（值已是 design token）；后续可按需改成 Sens 命名，非阻塞。
- `buildAntdTheme` 仍注入 antd 宿主；业务代码已不再 runtime 读 antd token。
