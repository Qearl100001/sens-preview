# 设计还原产线 · 更新日志 · 8月12日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## Product Shell · 固定高度样板间 + 滚动收起顶导

- [决策] 新增样板间 `/templates/product-shell` 验证固定高度产品壳；内容区 `scrollTop > 300` 收起顶导（含氛围），`≤ 300` 恢复；阈值暂定 300，待设计精准确认。`/composite/product-shell` 仍只承载规则页。
- [实现] `ProductShellTemplatePage` + `product-shell-template.css`：复用 `SensTopNavigation`（智能运营）/ `ProductShellSideNavigation` / `SensPageTitleBar`；收起态 `translateY(-82px)` + `padding-top` 同步；标题栏在内容列顶部、不进滚动容器。
- [实现] `PreviewShell` 对该路由 `overflow:hidden`，保证只滚内容区。
- [实现] `product-shell.md` §5 写入固定高度 / 阈值 / 收起规则；`top-navigation.md` 交叉引用。
- [验收] ✅已验「Playwright：Content overflow=hidden；scrollTop=350 → collapsed + paddingTop=0 + transform translateY(-82)；scrollTop=100 → 恢复 82 / identity；标题栏在滚动容器外」

## Product Shell · 层级 / 侧导 groups / 分页 / 回到顶部

- [决策] 对齐 `layout.md` §2.2：顶导氛围为底板，侧导与内容叠上；导航层展开态不设 `z-index`、不常驻 `transform`，避免困住顶导浮层。侧导改 Figma `3709:3989` 二级带图标 `groups`；`sf-*` 图标本轮占位。
- [实现] 侧导 `groups`（营销策略 / 资源位运营 / 微信互动配置 / 场景赋能）；`SensPagination` 演示第 4 页 + 跳至；内容面板右下 `SensButton fab` + `to-top` + `SensTips`「回到顶部」，距边 `spacing/6x`。
- [验收] ✅已验「Playwright：navZ=auto/workspaceZ=1；智能运营下拉压住内容；侧导含营销策略/资源位运营/列表资源位；分页 61-80·第4页·跳至；scroll>300 收起+回到顶部 24/24·36×36；点击滚回顶」

## Product Shell · 内容 24px / 标题栏滚动 D4 / 列表资源位卡片

- [决策] 内容模块左右 padding 对齐 Layout T 型 `spacing/horizontal/6x`（24）。内容 `scrollTop > 0` 时固定标题栏底出向下 D4。列表卡片按 Figma `3709:3196` 还原（样板间专属结构 + Sens 标签/按钮/徽标）。
- [实现] `ProductShellTemplatePage` / CSS：`SensTag` 原野黄待处理、`SensMoreButton`/`SensButton` small secondary、灰底信息区字段与近 7 日指标；标题栏 `title-bar--elevated` + `buildShadowD4()`。
- [token] `spacing/horizontal/6x` · `radius/l|m` · `outline-color-transparent` · `background-transparent-grey` · `text-article-color-transparent` · `link-color` · `buildShadowD4`
- [验收] ✅已验「Playwright：列表 L/R padding=24；卡片 radius=6 pad=16；含策略待处理/详情/更多/场景类型/近7日；scroll>0 标题栏 D4，回顶 shadow=none」

## Product Shell · 标题栏与工具区间距 0

- [决策] 对齐 Layout T 型：标题栏与首个工具区间距 `0px`（不新增 token）。
- [实现] `product-shell-template.css` 工具栏 `padding-top: 0`；`product-shell.md` §5 回写。
- [验收] ✅已验「Playwright：标题栏底到搜索框顶 gap=0」

## 侧导航 · 第 5 列图标全量入库 + 样板间接 `sf-*`

- [决策] 按 Figma `设计系统 2.1 侧导航设计` 表格第 5 列（`1809:123422`）补齐缺失侧导双色图标；命名归一化为 kebab-case；已入库 12 个不覆盖。智能运营样板间接正式 `sf-*`。
- [实现] 新增 43 个资产：`src/ui/SideNavProductIcons.tsx` + `src/design-system/icons/side-nav/*.svg`；`types` / `registry` / `/basic-styles/icon` 样张分组更新；`ProductShellTemplatePage` groups 改为 `sf-active-marketing` / `sf-section` / `sf-wechat` / `sf-scene`。
- [token] 列表卡片圆角/字色复核仍走 `radius/l|m` · `text-color-transparent@0.9` · `text-sub@0.58` · `text-article@0.74` · `link-color`（无硬编码 hex）。
- [验收] ✅已验「Playwright：`/templates/product-shell` 含营销策略/资源位运营/微信互动配置/场景赋能；侧导 SVG viewBox 含 `0 0 60 60`；卡片 borderRadius=6=`radius/l`；`--shell-text`=rgba(23,28,38,0.9)」

## Product Shell · 域导航同源（顶导下拉 ↔ 侧导）

- [决策] 推翻「侧导不反向推断顶导 / 智能运营顶导单层」：同一功能域顶导下拉与侧导必须同源 IA；侧导二级+三级 ⇒ 顶导 `grouped`；叶子选中双向对应；触发器文案仍为功能域名。权威模型为侧导 `groups`，顶导由 `domainNavToTopDropdownSections` 派生，禁止样板间另写扁平硬编码。
- [实现] 新增 `product-shell-domain-nav.ts`；`SensTopNavigation` 支持 `navDropdownByLabel` / 受控 `activeNavMenuByLabel` / `onNavMenuSelect`；`/templates/product-shell` 智能运营用 `SMART_OPS_DOMAIN_NAV` 同源驱动。
- [文档] `product-shell.md` §4、`top-navigation.md`、`side-navigation.md` 回写对齐规则。
- [验收] ✅已验「Playwright：智能运营下拉四 section（营销策略/资源位运营/微信互动配置/场景赋能）与侧导一致；无旧扁平「用户标签」；侧导点粉丝管理 → 顶导 menuitem aria-current；顶导点内容管理 → 侧导绿底选中 + 标题同步」

## Icon · 侧导资产移出线性 Tab → 导航功能图标

- [决策] 样张归属：Product Shell 侧导专用图标只进「导航功能图标」Tab，不进「线性图标」；录入标准写入 `docs/foundations/icon.md` §11.1。
- [实现] `IconBasicStylePage` 线性画廊排除 `NAVIGATION_FUNCTION` 线性名 + usageScenes 以 `Product Shell Side Navigation` 开头的条目；文案计数区分四类 Tab。
- [文档] `icon.md` 新增 §11.1 样张 Tab 录入标准；原风格表改 §11.2。
- [验收] ✅已验「Playwright：线性 Tab 无 sf-/sdh-/sa-/side-nav-expand 等侧导名（367）；导航功能 Tab 含营销策略/元数据管理/行为分析等；设计说明含 §11.1 样张 Tab 录入标准」

## Product Shell · 多域侧导 + 顶导 / 右上角切换

- [决策] 样板间顶导：`可视化` 替 `报表`；新增 `营销助手`；`广告投放分析` 替 `渠道追踪`；去掉 `数据治理` / `用户管理` / `项目设置`；`数据融合` 与 `数据加工` 为两个独立第一层（加工含分群/标签）。审批 / 资源 / 平台走右上角 utility。
- [实现] 扩展 `product-shell-domain-nav`（flat/groups + entry）；`product-shell-template-domains.ts`；`ProductShellTemplatePage` 同源切换；`onActiveNavLabelChange`。
- [验收] ✅已验「Playwright：顶导含可视化/营销助手/数据融合/数据加工/广告投放分析且无用户管理/数据治理；数据加工→分群管理；审批 utility→待办；数据融合侧导含埋点/通用/元数据/质量」

## Product Shell · 去掉概览 + 侧导分组常开

- [决策] 样板间顶导去掉「概览」；侧导二级分组默认全部展开且不可收起（无开合箭头）。
- [实现] `groupsCollapsible={false}`；移除 `OVERVIEW_DOMAIN_NAV`；`side-navigation.md` 回写。
- [验收] ✅已验「Playwright：顶导无概览且以可视化开头；营销助手六组+叶子全显；侧导 HTML 无 side-nav-up/down 分组箭头」

## Top Navigation · 点击一级不刷新落地页

- [决策] 悬停/点击均可打开下拉；点击一级触发器不得把叶子重置为域内第一项，永远保留该域上次选中的功能落地页。
- [实现] 样板间 `domainLeafByLabel` 记忆；同域再点一级 no-op；顶导带下拉且已在本域时只开菜单不 `selectPrimary`。
- [验收] ✅已验「Playwright：可视化选管理中心后两次点击可视化触发器，标题与菜单选中仍为管理中心」

## Top Navigation · 与样板间共用唯一域 IA 源

- [决策] 顶导只维护一套导航：`product-shell-nav-catalog` 为一级项 + 下拉唯一源；`SensTopNavigation` 默认与 `/templates/product-shell` 同源派生，禁止组件内另写业务扁平菜单。
- [实现] 目录迁入 `src/design-system/product-shell-nav-catalog.ts`；默认 `items` / `DEFAULT_NAV_DROPDOWN_CONFIG` / 初始叶子由 catalog 生成；样板间改引用；`top-navigation.md` / `product-shell.md` 回写。
- [验收] ✅已验「Playwright：基础页可见+overflow=10 与 catalog 一致；智能运营四 section；无概览；样板间同源」

## Top Navigation · 「更多」固有树换新 IA + 收纳量宽防叠字

- [决策] 保留「更多」贴右、从右往左收纳、三层级联、叶子回填等既有规则；只把固有树功能点换成 `product-shell-nav-catalog` 一级域（去掉概览/用户管理/渠道追踪/数据治理等旧点）。量宽改为主行 `clientWidth`，IA 变更重测，量宽未完成时主行暂裁切，避免主项叠到「更多」。
- [实现] `SensTopNavigation`：`MORE_MENU_TREE` / `moreMenuRoots` 同源派生；`primaryLabelsKey` 重置量宽；`ResizeObserver` 观察主行区；`top-navigation.md` 回写。
- [验收] ✅已验「Playwright：`/components/top-navigation` overflow=4；可见六项无互叠、不压「更多」；更多 L1 前置广告投放分析/场景库/数据融合/数据加工且无用户管理/渠道追踪；悬停广告投放分析 L2=概览/报表/推广/资产/管理与配置/工具」

## Top Navigation · 更多选叶同步 Product Shell 侧导

- [决策] 「更多」回填规则保留；选叶若映射到一级功能域（含窄屏收纳项），须经 `onNavMenuSelect` / `onActiveNavLabelChange` 换侧导；父级回传 `activeNavLabel` 时不得冲掉「更多」回填态。
- [实现] `selectMoreBusinessDestination` 解析域叶子并回调；`morePinnedLabelRef` 保住更多选中源；`top-navigation.md` / `product-shell.md` 回写。
- [验收] ✅已验「Playwright：`/templates/product-shell` 更多→分群管理 ⇒ domain/侧导=数据加工且回填分群管理；更多→数据源查询 ⇒ 数据融合；点智能运营恢复「更多」」

## Agent Rules · 页面校验清单补 Product Shell 防假通过项

- [决策] 将本轮顶导/样板间复盘中「易假通过」项写入 `page-evaluation-checklist` §6 / §6.1：双入口、收纳不叠字、IA 同源、更多选叶闭环、选中源分验、结论拆分；动效时序仍指向 `top-navigation.md`，清单不重复抄细则。顺手删除空的重复「§6 TikTok」标题。
- [实现] `docs/agent-rules/page-evaluation-checklist.md`。
- [验收] ✅已验「对照清单完成样板间+基础页复验；§6.1 主路径通过」

## Top Navigation · P1 收口：九宫格 IA / 业务浮层互斥 / 时序与矩阵

- [决策] 九宫格与矩阵示意叶子对齐 catalog（修正「用户路径分析」错字、去掉未映射「预警管理」、单层示意去掉「用户标签」）；第二行业务浮层互斥——开九宫格关主导航/更多，开主导航/更多关九宫格（项目/账号仍可并存）；补 `data-top-nav-function-trigger` 便于验收。
- [实现] `SensTopNavigation.tsx`（`FUNCTION_MENU_*`、互斥 hover）；`top-navigation.md` 回写。
- [验收] ✅已验「Playwright：九宫格→事件分析/自定义查询换域；分析↔九宫格↔更多互斥；离开 50ms 仍开、250ms 已关；点外立即关；1280/1440/1920 无叠字且 scroll>300 收起；矩阵 nine/grouped/flat 无旧错字」

## Product Shell · 列表卡「近 7 日」帮助图标对齐与色

- [决策] 帮助图标与标签文字垂直居中对齐；色走中性图标主色 `icon-color-transparent`（非继承副文案色）。
- [实现] `product-shell-template.css` `__help` 槽位；`--shell-icon`；`ProductShellTemplatePage` Tips 触发器改用该槽位。
- [token] `icon-color-transparent` · `size/icon/m` · `line-height/m`
- [验收] ✅已验「Playwright：帮助槽高=line-height/m；与标签中线 delta=0；color=`icon-color-transparent`」

## 待办 · 未完成欠账（仅写在最新一天）

- 顶导收起阈值 `300` 待设计确认是否升为正式口径。
- 列表资源位卡片是否升为独立复合组件待定（当前仅样板间）。
- `/basic-styles/icon` 新侧导分组浏览器逐个目视未做。
- 数据加工分群/标签等专用图标仍用 `default` 占位，待正式资产。
