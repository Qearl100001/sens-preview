# 设计还原产线 · 更新日志 · 6月23日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## 横切 · 建立更新日志写入纪律

- [决策] 日志长期滞后，根因不是「谁写」而是「无触发钩子 + 规则是孤儿」：`changelog/README.md` 本有维护说明，但 Cursor 收尾实际会读的横切文件（`review-checklist` / `conventions` / `how-cursor-works` / 知识库索引）全不提 changelog，索引也未收录，导致干活时读不到。改为「把纪律挂进收尾必读路径 + 显式开 markdown 例外」根治。
- [决策] 冻结点有二：① 无触发钩子（何时写）；② 写了但忘注册预览页（写了不可见）。本轮分别用「纪律进收口路径」和「glob 自动扫」各解一个。
- [实现] 扩 `changelog/README.md`（写法模板 / 触发阈值 / 责任方 / DoD 四块）；新建 `.cursor/rules/changelog-write-discipline.mdc`（`alwaysApply: true`，与 `token-variable-first.mdc` 叠加不冲突，各管组件变量化 vs 日志纪律）；`review-checklist.md` 加收口必过项（changelog 已补 + 同 commit）及 markdown 例外；`design-system/README.md` 收录 changelog 子体系并在维护原则末条补指针。
- [实现] `ChangelogPage.tsx` 由硬编码三天 `?raw` import 改为 `import.meta.glob("../../design-system/changelog/20*.md")`：`20*.md` 排除 `README.md`；`key` 取 ISO 日期倒序；`label` 用 `N月D日`（`Number` 去前导零，避免 Intl 出 `6/22`）；空目录时 Segmented 不渲染并提示新建 md。
- [决策] 与用户既有规则「勿编辑未要求的 markdown」冲突，显式开例外：**仅** `src/design-system/changelog/` 目录 Cursor 可在用户未点名时主动更新；`conventions` / `how-cursor-works` 本轮 deliberately 不改，避免纪律长文混入横切规范，入口集中在 changelog/README + alwaysApply mdc + review-checklist 收口。
- [决策] commit 口径定死：changelog 与其记录的代码改动进**同一 commit**，不留「用户指定」口子（纪律三处文档 + mdc 均已写死）。
- [验收] ✅已验「Playwright 打开 /changelog，逐个点击 6/17、6/18、6/22」：Segmented 自动列出、标签 `N月D日` 格式正确、卡片标题与正文渲染正常，控制台无 page error；无需手动注册 `CHANGELOG_ENTRIES`。

## 按钮 · padding 落地（横向 padding 与 Input 同源）

- [决策] 大按钮左右 padding 由 antd 默认推导的 15px 收到 `spacing/horizontal/3x`=12，小按钮由 7px 调到 `SP_H_2_5`=10，与 Input 的 `paddingInline` / `paddingInlineSM` 完全同源，横向间距口径自此统一。横切尺寸改动，单独做、单独验。
- [实现] `build-tokens.mjs` → `components.Button` 增 `paddingInline: U[SP_H_3]`、`paddingInlineSM: U[SP_H_2_5]`（与 `INPUT_FIELD_TOKENS` 同源）；`theme.ts` 已含 12/10，antd 按 `size` 自动分配，无 CSS。
- [token] `spacing/horizontal/3x` → `paddingInline` 12；`spacing/horizontal/2․5x`（`SP_H_2_5`）→ `paddingInlineSM` 10。
- [验收] ✅已验「DevTools computed + Playwright 截图」：大按钮（32）左右各 12px、小按钮（24）各 10px；纯图标与 `SensDropdownButton` 未歪；`CategorySearchInput`（常规/简约）+ `CategorySearchTriggerInput` 四态接缝仍成立（触发型 affix→button gap -1px；常规分类 input `margin-inline-start: -1px`），无新缝、无双描边。

## 按钮 · 横向单项 FAB（`fab=true`）

- [决策] 悬浮按钮与常规 Button 几何/投影口径分离：`fab=true` 时圆角 `radius/circular`（999）、全态恒 D4 投影；二级走 `variant=text` 白底无描边，锁死 antd `textHoverBg` 灰底不漏出。不扩 `components.Button.paddingInline`（须保持 12 与 Input / 搜索缝同源）。
- [实现] `SensButton.tsx` 增 `fab?: boolean` + `buildFabToneProps`；`button.css` `.sens-btn-fab` / `.sens-btn-fab-secondary` 白底与 `data-fab-hover` / `data-fab-active` 字色；`ButtonStatesPreview` 增「横向单项 FAB」矩阵（一级/二级 × 纯文字/图文/纯图标 × 7 态）；i18n `sensd-button-fab-*`。
- [token] `radius/circular` → 圆角 999；`mask-01-transparent` → D4（`buildShadowD4`）；二级字色 `text-color` / `component-hover` / `component-active`。
- [验收] ✅已验「Playwright computed `/components/button`」：二级 FAB 五态 + live hover 背景均 `rgb(255,255,255)`；FAB 圆角 999、常规 Button 仍 4px；一级绿底白字 / 二级白底黑字 hover 转绿；恒 D4；常规分支零变化。

## 按钮 · FAB 共用层（`fabShared.ts`）

- [决策] 单项与组合 FAB 配色/几何抽共用模块，防两套实现漂移；`SensButton` 仅改 import 路径，单项对外行为不变。
- [实现] 新建 `src/ui/fabShared.ts`：`buildFabToneProps`、`getFabRadiusStyle`、`getFabCssVars`、组合分段 helper、预览快照色；`SensButton.tsx` 从本地逻辑迁出。
- [验收] ✅已验「抽层后横向单项 FAB 圆角/D4/padding 与抽前一致」。

## 按钮 · 横向组合 FAB（`SensFabGroup`）

- [决策] 2~3 段组合不用 `Space.Compact`（`-1px` 为有描边控件消双线，填色块齐平用不上）；自定义 `inline-flex` 容器 + 裸 `antd Button` 分段，整组一层 D4、子段 `boxShadow: none`；每段显式 `borderRadius`（外端 999 / 内缝 0），接缝须单独验。
- [实现] `SensFabGroup.tsx` + `fab-group.css`；`FabGroupStatesPreview`；`ButtonShowcasePage` 经 `afterFabSection` 插在「横向单项 FAB」与「下拉」之间；`index.ts` 导出。
- [token] 外 padding `spacing/horizontal/5x`（20）；内 padding `spacing/horizontal/2․5x`（`SP_H_2_5`，U+2024 key）；圆角 `radius/circular`；投影 D4。
- [验收] ✅已验「Playwright computed」：容器 D4、分段 shadow `none`；二级组合 hover/active/disabled 白底；3 段中段 hover 接缝无 1px 错位/绿线；padding 外 20 / 内 10；一级绿底白字 hover 变深、二级白底黑字 hover 转绿；2 个/3 个宽度均对；单项 FAB 与常规 Button 零变化。

## 按钮 · FAB 尺寸收口（padding 20 + 高度 36，scoped）

- [决策] 单项 FAB 横向 padding 不继承全局 12，scoped 覆盖为 `spacing/horizontal/5x`=20（纯图标 circle 不受影响）；FAB 交叉轴规格为 36 而非常规 `controlHeight`=32，读已有 token、不改 `theme.ts`。
- [实现] `fabShared.ts`：`FAB_SINGLE_PADDING_HORIZONTAL`、`getFabSinglePaddingStyle`；`FAB_COMPONENT_HEIGHT_PX`（`size/component-height/l`）→ `--sens-fab-height`；`getFabCrossAxisStyle` / `getFabGroupSegmentCrossAxisStyle`；`button.css` / `fab-group.css` 读 CSS 变量。
- [token] `spacing/horizontal/5x` → 单项左右 padding 20；`size/component-height/l` → FAB 高/宽 36；`components.Button.paddingInline` **仍为 12**。
- [验收] ✅已验「Playwright computed」：单项文字/图文 padding 各 20、高 36；纯图标 36×36 正圆；组合分段高 36、padding 20/10 不变；常规 Button 高 32、padding 12；组合 D4/圆角/接缝零回退。

## 按钮 · 竖向组合 FAB（`direction="vertical"`）

- [决策] 与横向组合同源几何（D4 在容器、分段无投影、2~3 段、内外 padding 20/10），但配色/内容独立：锁二级纯图标、`variant=text` 白底；图标色走 `icon-color-transparent` → `component-primary`（hover，**非**横向 `component-hover`）→ `component-active`；仅 default/hover/active 三态，无 disabled/loading。
- [实现] `SensFabGroup` discriminated union（`direction: 'vertical'`）；`FabVerticalGroupSegment` + `FabVerticalGroupStatesPreview`；`fabShared` 几何 `direction` 参数化、竖向色阶 `getFabVerticalIconCssVars`；图标显式绑 `size/icon/m`。
- [token] 交叉轴宽 `size/component-height/l`（36）；图标 `size/icon/m`（16）；padding 复用 `FAB_GROUP_PADDING_OUTER` / `INNER`；圆角 `radius/circular`。
- [验收] ✅已验「Playwright computed」：横向零回退（组合 36/20·10/D4、单项 36、常规 32）；竖向容器宽 36、图标 16×16；首/中/末 padding 顶底 20·10 模式、左右 0；上下端 999 内缝 0；hover 图标 `component-primary`、默认 `icon-color-transparent`、点击 `component-active`；`variant=text` 抢色已用 wrap + `setProperty` 兜底。
