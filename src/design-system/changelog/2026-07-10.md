# 设计还原产线 · 更新日志 · 7月10日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## Theme / Skinning · Functional Color Token Mapping 落库

- [决策] 换肤继续拆成 Navigation Theme 与 Functional Color Theme 两套独立主题；本轮**只落映射表**，不实现换肤，**不手改** `tokens.resolved.json` / `theme.ts`。状态色默认不纳入功能色换肤；链接先列入功能色映射表，换肤列标 To Confirm。
- [实现] `docs/foundations/theme-skinning.md` 增补 §11 生成链路、§12 Functional Color Token Mapping（主操作 / 选中投影浅底 / 链接 / 开关 / 标签 / 便签 / 状态色）、§13 与 Navigation 边界、§14 待补；`ThemeSkinningBasicStylePage.tsx` 同步展示映射表与汇总；`docs/foundations/README.md` 更新 `theme-skinning.md` 状态。
- [token] 映射引用既有 handle：`component-primary` / `component-hover` / `component-active` / `component-disable*` / `component-active-*-background` / `component-active-shadow` / `component-light-background` / `link-*` / `switch-background*` / `tag-default-background` / `tooltip-background`；未新增源 token，未重跑 `build-tokens.mjs`。
- [验收] 🔶待验「打开 `/basic-styles/theme-skinning`：左侧可见 Functional Color Token Mapping 与汇总；右侧文档含 §12；确认未改生成物」

## Tag · API 骨架草稿落库

- [决策] 标签采用**一个 `SensTag` + props**；状态标签在 Tag 内单独 `variant="status"`；尺寸档位先空，等 Figma 4.2 核对后再写入属性表。
- [实现] 新增 `src/design-system/components/base/tag.md`（六种类型→属性对照、API 骨架、与 Badge 边界、待补清单）；`docs/references/source-index.md` 登记标签 Figma 专档并更新缺口说明。
- [token] 未新增；引用既有 `定制色/标签/*`、`tag-default-background`；未改生成物。
- [验收] 🔶待验「读回 `tag.md`：决策与对照表与对话确认一致；无 `SensTag` 实现属预期」

## Tag · 4.2 / 4.3 尺寸与间距写入

- [决策] `size: "large" | "small"`，默认 `large`；大 24/14、小 20/12；组间距 4px；文案 max 112/96；样张 2× 需 ÷2。`status` 是否共用 pill 高度标 To Confirm。
- [实现] 更新 `tag.md`（尺寸与间距节、对照表、API）；同步 `docs/foundations/size.md` §5 为大/小双档。
- [token] 高度/字号挂既有 `size/component-height/s|xs`、`font-size/m|s`、`spacing/1x`、`radius/s`；padding / max-width / close 仍为组件专属待补；未改生成物。
- [验收] 🔶待验「读回 `tag.md` 尺寸表与 Figma 4.2/4.3 正文一致；`size.md` §5 含 large 档」

## Tag · P0 组件与预览挂载

- [决策] 本轮只做 P0：`SensTag` + `/components/tag`；侧栏文案「标签」；`status` 先圆点+文案；不做全交互矩阵、不换肤、不改生成物、不替换业务 beta。
- [实现] 新增 `src/ui/SensTag.tsx`、`TagShowcasePage.tsx`；`PreviewShell` / `App` 挂路由；更新 `tag.md`、`source-index.md`。
- [token] 多色 `getColorByPath(定制色/标签/…/背景/01_默认)`；叠加 `tag-default-background` @0.65；中性 `background-01-transparent` @0.08；尺寸挂既有 unit / typography token。
- [验收] 🔶待验「打开 `/components/tag`：侧栏可见「标签」；Demo 可切样子/色/尺寸/可点可关；矩阵含六种类型大/小」

## Tag · 状态五语义收口

- [决策] 状态标签仅五语义（成功/进行中/异常/失败/失效），色固定不可换；无点击、可移除、禁用；圆点与文案同色。
- [实现] `SensTag` 增 `status` prop，status 分支忽略交互 props；Demo 选状态时改「语义」下拉并隐藏无关开关；矩阵展示五语义大/小；更新 `tag.md`。
- [token] `success-color` / `link-color` / `info-color` / `warning-color` / `text-color-disable`；未改生成物。
- [验收] 🔶待验「`/components/tag` 选状态：无关闭钮；语义五档圆点与文字同色」

## Tag · 状态色校正（中性字 + 进行中外描边）

- [决策] 文案一律中性 `text-color-transparent`；圆点走状态色 handle；仅 `processing` 圆点加 `link-color` @20% 外描边（`0 0 0 2px`），其余语义无描边。
- [实现] 更新 `SensTag.tsx` status 分支；同步 `tag.md` 语义表。
- [token] 圆点 `success-color` / `link-color` / `info-color` / `warning-color` / `text-color-disable`；文案 large `text-color-transparent` @0.9、small `text-sub-color-transparent` @0.58；外描边 `tokenRgba("link-color", 0.2)`；未改生成物。
- [验收] 🔶待验「`/components/tag` 状态：大字主文字、小字辅助色 @58%；进行中蓝点有浅环；其余无环」

## Unit · 补 spacing/1.5x 与 size 缺口并接入 Tag

- [决策] 通用档进 `tokens/source/figma/unit.json`（非 `tag/` 前缀）：`spacing/1.5x=6`、`size/2xs=6`、`size/icon/xs=12`；Tag 高度固定、无上下 padding；左右 padding / 关闭 / 圆点全部挂 unit token。
- [实现] 更新 `unit.json`；`build-tokens.mjs`；`SensTag`；顺带 Select/Dropdown 浮层上 6px 改读 `spacing/1.5x`；同步 spacing/size 文档与样张。
- [token] `spacing/1.5x`（及 horizontal/vertical 别名）、`size/2xs`、`size/icon/xs`；Tag：`spacing/horizontal/2x|1.5x`、`size/mini`、`size/icon/s|xs`。
- [验收] 🔶待验「`/basic-styles/spacing` 可见 1.5x=6；`/components/tag` 大/小高度 24/20、左右 8/6、无上下 padding」

## Tag · 多色交互态

- [决策] 彩色标签为固定色板、**不参与** Functional 换肤；仅 `clickable` 切悬停/点击；接 `背景/02|03` + `文字&图标`；中性可点悬停/点击走冰绽蓝（Figma 画板 08）；默认文案 `text-color-transparent` @0.9。
- [实现] `SensTag.tsx`：`resolveTagInteractiveSurface` + 真实 mouse 态；预览 `previewState` 静态矩阵；`tag.md` / `theme-skinning.md` §12.5 同步「彩色不换肤」。
- [token] 六色 path `定制色/标签/{色}/背景/01|02|03`、`文字&图标/01|02`；中性默认 `background-01-transparent` @0.08；中性交互复用冰绽蓝 path；未改生成物。
- [验收] 🔶待验「`/components/tag`：可点 Demo 悬停/点击变色；矩阵色系×默认/悬停/点击；中性悬停为冰绽蓝」

## Tag · 移除图标交互态

- [决策] 关闭钮独立热区，不跟标签体多色悬停；色走既有 design token，不新增源。
- [实现] `resolveTagCloseColor` + 关闭钮 mouse 态；`previewCloseState` 矩阵（大/小 × 默认/悬停/点击/禁用/禁用悬停）；可点根为 button 时关闭用 span 避免嵌套。
- [token] 默认 `icon-color-transparent`（叠加 `white`）；悬停 `warning-color`；点击 `warning-color-active`；禁用 `icon-color-transparent-disable` @0.4；禁用悬停 `icon-color-transparent-disable-hover` @0.3；尺寸 `size/icon/s|xs`。
- [验收] 🔶待验「`/components/tag`：可移除 Demo 悬停关图标变红、按下变深红；矩阵移除五态大/小」

## 轻提示 / 警告 · 文档草稿分册落库

- [决策] **两个组件、两份 md、两个预览页**（侧栏「轻提示」「警告」）；不合并。`type`：`default|success|info|warning`（+ 轻提示 `loading`）；`info`=提醒、`warning`=警告红。场景写组件 md，不进 `color-semantics.md`。加载不新增色 token。
- [实现] 新增 `message.md`、`alert.md`；`source-index.md` 登记提示专档 + 大库 Message/Alert 节点；`design-system/README.md` 索引。本轮**未**实现 `SensMessage` / `SensAlert`。
- [token] 轻提示：图标 `success-color` / `info-color` / `warning-color` / 加载中性；底 `white` + D4↓。警告条：浅底/描边 `success|info|warning-light-background|outline`（常规当时待对稿）。
- [验收] 🔶待验「读回两份 md：命名与 type 枚举与确认一致；互不混写；无组件代码属预期」

## 轻提示 / 警告 · 常规色定稿

- [决策] 常规走链接状态色链：图标 `link-color`；浅底 `link-light-background`；浅描边 `link-light-outline`。轻提示仅用图标色，容器仍白底+D4（不用浅底/描边）。
- [实现] 更新 `message.md` / `alert.md` 决策表与语义映射表。
- [token] `link-color` / `link-light-background` / `link-light-outline`；未改生成物。
- [验收] 🔶待验「读回两份 md：`default` 行与上表一致」

## 轻提示 / 警告 · P0 组件与预览挂载

- [决策] 按确认方案落地两组件两页；关闭细交互与尺寸定稿留待补；不改生成物、不做全局 Message 队列。
- [实现] `feedbackShared.tsx` + `SensMessage` / `SensAlert`；`MessageShowcasePage` / `AlertShowcasePage`；`PreviewShell` / `App` 挂「轻提示」「警告」；同步 md / README / source-index。
- [token] 图标 `link|success|info|warning-color` / 加载 `icon-color-transparent`；Message 底 `white` + `buildShadowD4`；Alert `*-light-background` / `*-light-outline`；文案 `text-color-transparent` @0.9、辅助 @0.58；尺寸挂 `font-size/m`、`line-height/m`、`size/icon/m`、`radius/m`、`spacing/horizontal/3x`、`spacing/1x`；垂直 pad `FEEDBACK_PAD_BLOCK=7`（无 unit）。
- [验收] 🔶待验「侧栏打开 `/components/message` 与 `/components/alert`：Demo 可切 type；矩阵含各 type；常规图标为链接蓝；Alert 常规浅底浅蓝」

## 警告 · 辅助文案字号校正

- [决策] 对稿 Figma `4212:16045`：辅助文案走「中文/辅助信息」12/18，不与标题 14/22 同档。
- [实现] `SensAlert` description 显式挂 `font-size/s` / `line-height/s`；同步 `alert.md`。
- [token] `font-size/s`、`line-height/s`、`text-sub-color-transparent` @0.58。
- [验收] 🔶待验「`/components/alert` 开辅助文案：副文 12px/18，标题仍 14/22」

## Icon · 入库 close 并替换手写小叉子

- [决策] Figma `805:58` 正式入库为 Sens `close`；Tag / Message / Alert 一律改用 `SensIcon name="close"`；删除组件内手写小关闭 SVG。
- [实现] `CloseIcon`（FieldIcons）+ registry `close`；`SensTag` / `feedbackShared` 改 SensIcon；`icon.md` 登记；导出 `CloseIcon`。
- [token] 尺寸仍由场景传入（Tag `size/icon/s|xs`，Message/Alert `size/icon/m`）；色仍走既有 close 色规则。
- [验收] 🔶待验「`/basic-styles/icon` 可见 close；`/components/tag|message|alert` 关闭钮为 Sens 叉子，非旧小 X」

## 轻提示 / 警告 · 高度改走 size token

- [决策] 无辅助文案走整体高度：Message `size/component-height/m`（32）、Alert `size/component-height/l`（36）。有辅助不锁高，上下 pad = `(component-height/l − line-height/m) / 2`；不新增 5/7 spacing。标题↔辅助 gap 改 `spacing/1x`。
- [实现] 删 `FEEDBACK_PAD_BLOCK`；`messageContainerStyle` / `alertContainerStyle`；同步 md。
- [token] `size/component-height/m|l`、`line-height/m`、`spacing/1x`、`spacing/horizontal/3x`。
- [验收] 🔶待验「Message 高 32；Alert 单行 36；有辅助约 58 且副文 gap 4」

## 待办 · 未完成欠账

- 确认链接 / 开关开态 / 便签三类 To Confirm 后再扩展 `getFunctionalColors`（彩色标签已确认不换肤）。
- 补 Navigation Theme 完整换肤矩阵（侧导背景、标题栏、渐变 helper）。
- 建立组件换肤映射表（Ready / Half Ready / Missing / To Confirm）。
- 状态色是否允许主题化：当前默认不跟随。
- Tag：Tips；加载态；替换 `DataSourceEntryCard` beta；max-width 112/96 仍为专属数字。
- 轻提示 / 警告：关闭悬停红、链接细态、Alert 默认宽 To Confirm；状态图标是否入库 Sens。
