# 设计还原产线 · 更新日志 · 7月3日

> 记录对「设计还原产线」和 `src/design-system` 知识库做的事。

## 数据源接入样板间 · Google Ads

- [决策] 基于已有 TikTok Ads 下钻页结构，通过新增 `GOOGLE_ADS_SPEC` 字段描述生成 Google Ads 接入页，证明同结构样板间可复用。
- [实现] `dataSourceSpecs.ts` 新增 spec 与可导航卡片；`dataSourceDemoConfig.ts` 按 `sourceId` 返回 mock；`DataSourceConnectionDemoPage.tsx` 扩展 spec 路由表。
- [token] 无新增样式；页面继续复用 `DataSourceConnectionPage`、`CreateConnectionDrawer` 等既有组件与 token。
- [验收] ✅已验「`npm run build` 通过；Playwright 走通 Google Ads 管理页→下钻→创建校验→新增行→搜索→返回；TikTok 下钻未回归破坏」
