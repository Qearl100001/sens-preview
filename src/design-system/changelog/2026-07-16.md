# 设计还原产线 · 更新日志 · 7月13日-7月16日

> 记录 7月13日到 7月16日对「设计还原产线」和 `src/design-system` 知识库做的事。

## 里程碑 · Design System MD 前置补齐

- [决策] 短期先补齐基础组件、场景规则、token 映射和可验收 HTML；下周再把稳定内容抽取进 `DESIGN.md`，不急于合并文档。
- [实现] 按 P0 优先级推进 Navigation、Table、Pagination、Checkbox、Radio、Title、Form 等基础资产，并把组件文档与预览页同步维护。
- [验收] ✅已验「本阶段多轮 `npm run build` 通过；关键页面在浏览器完成检查」

## Foundation · Navigation Color / Theme Skinning / Grid / Layout

- [决策] Product Shell Theme 与 Functional Skin 继续拆开；导航颜色不复用功能色，功能色组件才跟随 Functional Skin。
- [实现] `Navigation Color` 补齐顶导、侧导、标题栏、页面背景与产品壳菜单映射；新增 `theme-top-atmosphere` 与导航背景 helper。
- [实现] `Layout` 与 `Grid` 拆分：Layout 管页面骨架、左侧区域 Overlay / Docked / Reflow；Grid 管 20 栏、12 栏和局部容器排布。
- [token] 通过 `tokens/source/foundations/navigation-theme.json` + `node build-tokens.mjs` 生成 `tokens.resolved.json` / `theme.ts`，不手改生成物。
- [验收] ✅已验「`node build-tokens.mjs`、`npm run build` 通过；Navigation Color / Theme Skinning / Grid / Layout 页面可打开」

## Composite · Top Navigation / Side Navigation

- [实现] 顶导完成项目切换、主导航选中短线、账号/功能菜单、工具图标态和导航氛围层；侧导完成 Normal / Overlay / Docked 三态与投影方向。
- [token] 顶导消费 `theme-top-*`，项目菜单消费 `theme-top-proMenu-*`，账号/功能菜单消费 `theme-top-funcMenu-*`；侧导消费 `theme-side-*` 与 `getThemeSideBackground()`。
- [坑] 顶导主导航选中短线不能误用绿色 `theme-top-menuLine-active`；侧导「展开」不等于「选中」。
- [验收] ✅已验「浏览器检查顶导项目切换、平台选中、账号菜单；侧导 Normal / Overlay / Docked 宽度与投影方向；控制台无 error」

## Base Component · Table / Pagination

- [决策] 表格与分页器拆成两个基础组件；筛选表格、录入型表格、树表格另进复合组件或业务样板间。
- [实现] `TableShell` 补表格信息区、批量操作、操作列、空态 / 加载 / 滚动规则；旧 `composite/table.md` 拆出到 `base/table.md`。
- [实现] `SensPagination` 补常规 / 简约两种结构、10 页以内平铺、超过 10 页折叠页码、hover tips、每页条数选择器与跳页输入。
- [token] 表格 / 分页器颜色、间距、字号、圆角、图标全部映射 SensD token；antd 只作为底层承接，不作为设计来源。
- [验收] ✅已验「`/components/table`、`/components/pagination` 浏览器检查；`npm run build` 通过」

## Base Component · Checkbox / Radio

- [决策] 单选和复选共用 Figma 资料，但代码、文档和预览分开；单选组 / 复选框组作为基础组件能力录入。
- [实现] 新增 `SensCheckbox` / `SensCheckboxGroup`、`SensRadio` / `SensRadioGroup`，覆盖默认、hover、点击、禁用、禁用悬停、已选 / 半选等状态。
- [实现] 禁用态下主文案和辅助文案同步变为禁用色；带辅助文案的选项不额外补 32px 对齐盒。
- [token] 使用 `component-primary`、`component-hover`、`component-active`、`component-active-shadow`、`text-color-transparent*`、`spacing/*`、`size/icon/m`。
- [验收] ✅已验「`/components/checkbox`、`/components/radio` 页面可打开；`npm run build` 通过」

## Base Component · Title / Form

- [决策] 标题组件独立录入；表单只引用标题在表单项 / 分组中的使用关系。业务表单样板间后续放到「业务样板间 > 表单」。
- [实现] 新增 `SensSectionTitle`，补通用标题、专用标题、大小尺寸、圆角、右侧操作与帮助图标槽位。
- [实现] 新增 `SensForm` / `SensFormItem` / `SensFormActions`，补上下布局、左右布局、标题顺序、选填规则、标题省略、单选复选对齐、文本域对齐、基础间距。
- [实现] 表单报错和辅助信息共用一个信息槽；有报错时替代辅助信息，结构为 `feedback-error` 图标 + 报错文案。
- [token] 表单间距使用 `spacing/vertical/*` / `spacing/horizontal/*`，标题与辅助文案使用 Typography token，报错使用 `warning-color`。
- [验收] ✅已验「`/components/title`、`/components/form`；Form 页面 3 条报错都有 SVG 图标，报错 meta 不再混入辅助信息；控制台无 error」

## Icon / Feedback · Alert / Message 状态图标

- [决策] Alert / Message 前置状态图标必须来自 SensD icon registry；loading 资产暂未录入，继续标 Missing。
- [实现] 新增 `feedback-info`、`feedback-complete`、`feedback-warning`、`feedback-error` 四个状态图标；Alert / Message 替换旧 antd filled status icon。
- [token] 图标使用 `currentColor`，颜色由 `link-color`、`success-color`、`info-color`、`warning-color` 控制。
- [验收] ✅已验「`/components/alert` 旧 antd filled 状态图标为 0；`/components/message` 非 loading 状态已替换，loading 保留临时 spinner」

## Form / Title · 表单基础设计原则 `64:55048`

- [决策] 将 Figma「表单的基础设计原则」五条写入 Form；灰条/绿条边界与辅助文案基线对齐落到 Title；通用小尺寸灰条标题保留但大表单页默认不用。
- [实现] `form.md` 新增「基础设计原则」；`form.design.md` 登记 `64:55048`；`title.md` / `title.design.md` 补二级灰条绿条规则与基线规则；`SensSectionTitle` 的 `description` 进入标题行并 `align-items: baseline`；`/components/title` 增加带辅助文案基线样张。
- [token] 辅助文案继续 `text-sub-color-transparent` @58% + `font-size/s`；标题行间距 `spacing/horizontal/1x`；不新增源 token。
- [验收] 🔶待验「浏览器打开 `/components/title` 与 `/components/form`：五条原则可见；二级标题+辅助文案同一行且基线对齐」

## Form · 分组规则 `69:26542`

- [决策] 只有分组树最顶层（16px）带灰条/绿条；第二层 16px 无条、第三层 14px 无条；不够再用白描边卡，卡内只可再套灰底卡且不可再出白描边；白卡内灰条可重置层级；灰卡内标题仅 14px，16px 暂无场景。
- [实现] `form.md` 新增「分组规则」；同步修订原则 1 表述与 `title.md` / `title.design.md` / `form.design.md`；图 1/2/3 完整结构样张留样板间。
- [token] 标题字号 `font-size/l`（16）/ `font-size/m`（14）；分组间距继续 `spacing/vertical/10x` / `7x` / `5x`；卡片样式引用 Card Foundation，不新增源 token。
- [验收] 🔶待验「`/components/form` 演示 Demo 顶部可见分组层级示意；第二层 16px 用 `font-size/l` token」

## 待办 · 未完成欠账

- 账号功能菜单图标组尚未录入 SensD，不能用错误图标替代。
- 表格 setting icon、加载 GIF / 动效资产仍有 Missing 项。
- Tooltip 基础组件尚未系统录入，当前部分提示仍先复用现有能力或原生 `title`。
- 表单第二层 16px 无灰条标题组件化、分组图 1/2/3 结构样张、业务样板间后续继续补。
- 筛选表格、录入型表格、树表格、SDH 场景页后续继续补。
- `DESIGN.md` 下周再从已稳定规则中抽取，不在本阶段强行合并。
