# 2026-07-28

## Tabs · 视觉 token 去 antd 中转

- [决策] antd 不再作为 Tabs 视觉 token 的消费桥；基础 / 页签标签页仅保留 antd 的结构与行为职责。
- [实现] `SensTabs.tsx` 直接读取 Sens color / typography / unit / shadow helper，并通过 `--sens-basic-tabs-*`、`--sens-pill-*`、`--sens-card-tab-*` 注入颜色、字体、间距、圆角和投影。
- [实现] Tabs 状态矩阵改为项目自持快照结构；`tabs.css` 不再读取 `--ant-*`，token 可表达的视觉值不再保留 hex / rgba / px fallback。
- [实现] 删除 `build-tokens.mjs` 的 `components.Tabs` 映射并重建 `src/design-system/theme.ts`。
- [边界] Editable Card Tabs 本轮未重写 rc-tabs 宿主；`.ant-tabs-*` 选择器仍用于匹配其 DOM，后续若继续出现高强度结构覆盖，再单独评估自持页签条。
