# 2026-08-04

## Figma 线性图标批量入库

- 从用户提供的 Figma 文件导入 10 组线性图标，共 340 个 symbol，规范化后保留 339 个唯一名称；同名节点 `newchat` 以 `newchat` / `newchat-2` 区分。
- SVG 作为静态资产放入 `public/icons/`，由 `SensIcon` 统一通过 CSS mask 渲染，支持 `currentColor` 和 16px / 20px / 22px 尺寸切换。
- 扩展 Icon registry、图标分类和中文语义；已存在的同名 Sens 图标由 Figma 资产优先承载，未迁移的历史资产继续保留。
- `/basic-styles/icon` 样张补充批量图标展示与 16px / 20px / 22px 尺寸切换；去掉图标卡片中的 SVG 来源信息。
- 更新 Icon Foundation：明确后续不新增 antd 图标，现有业务宿主按组件批次迁移。

## 验收

- `npm run build` 通过。
- 浏览器确认线性 / 面性两个图标 Tab 均可切换；面性 Tab 实际渲染 74 个 `/icons/filled/` mask 资产，16px / 20px / 22px 尺寸切换实际改变 mask 尺寸，页面无横向溢出。
- 340 个静态 SVG 文件均已生成；宿主组件的点击、拖拽、hover、active、disabled 仍待按组件逐项验收。

## Figma 面性图标补充

- 从用户提供的 9 组 Figma 面性图标分组导入 74 个 symbol，保留 `currentColor` 和 Figma 语义命名；其中 `up-number-input` / `down-number-input` 为 12px，其余主要为 16px。
- 面性资产放入 `public/icons/filled/`，通过 `SensIcon variant="filled"` 消费；线性图标继续作为默认 variant，不替换现有宿主消费。
- `/basic-styles/icon` 的「数值样张」分为「线性图标」和「面性图标」两个 Tab，共用尺寸切换。
