# AI 设计系统方法论（已并入 `DESIGN.md`）

这个文件不再作为 `sens-preview` 的主入口。

正式入口已经迁到仓库根目录：

- `DESIGN.md`

当前保留本文件，只是为了兼容旧引用和历史路径。

如果你正在处理这个项目：

1. 先读 `AGENTS.md`
2. 再读 `DESIGN.md`
3. 只有在需要回看历史命名或迁移背景时，再打开本文件

方法论内容已经整理进 `DESIGN.md` 的这些部分：

- 项目一句话
- 信息分层
- AI 设计系统方法论摘要
- 当前阶段重点
